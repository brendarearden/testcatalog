{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar381.serviceAccountName" . }}
  labels:
{{ include "bar381.labels" . | indent 4 }}
{{- end -}}
