{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar256.serviceAccountName" . }}
  labels:
{{ include "bar256.labels" . | indent 4 }}
{{- end -}}
