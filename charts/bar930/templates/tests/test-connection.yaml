apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar930.fullname" . }}-test-connection"
  labels:
{{ include "bar930.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar930.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
