{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1497.serviceAccountName" . }}
  labels:
{{ include "bar1497.labels" . | indent 4 }}
{{- end -}}
