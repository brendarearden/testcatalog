{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar476.serviceAccountName" . }}
  labels:
{{ include "bar476.labels" . | indent 4 }}
{{- end -}}
