{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo420.serviceAccountName" . }}
  labels:
{{ include "foo420.labels" . | indent 4 }}
{{- end -}}
