apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo420.fullname" . }}-test-connection"
  labels:
{{ include "foo420.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo420.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
