{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar911.serviceAccountName" . }}
  labels:
{{ include "bar911.labels" . | indent 4 }}
{{- end -}}
