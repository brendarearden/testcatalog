{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo54.serviceAccountName" . }}
  labels:
{{ include "moo54.labels" . | indent 4 }}
{{- end -}}
