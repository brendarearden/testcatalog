apiVersion: v1
kind: Service
metadata:
  name: {{ include "moo1141.fullname" . }}
  labels:
{{ include "moo1141.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "moo1141.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
