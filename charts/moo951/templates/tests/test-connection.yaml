apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo951.fullname" . }}-test-connection"
  labels:
{{ include "moo951.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo951.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
