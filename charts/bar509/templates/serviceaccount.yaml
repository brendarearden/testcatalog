{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar509.serviceAccountName" . }}
  labels:
{{ include "bar509.labels" . | indent 4 }}
{{- end -}}
