{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1041.serviceAccountName" . }}
  labels:
{{ include "foo1041.labels" . | indent 4 }}
{{- end -}}
