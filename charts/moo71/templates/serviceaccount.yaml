{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo71.serviceAccountName" . }}
  labels:
{{ include "moo71.labels" . | indent 4 }}
{{- end -}}
