{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar99.serviceAccountName" . }}
  labels:
{{ include "bar99.labels" . | indent 4 }}
{{- end -}}
