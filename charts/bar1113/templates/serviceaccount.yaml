{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1113.serviceAccountName" . }}
  labels:
{{ include "bar1113.labels" . | indent 4 }}
{{- end -}}
