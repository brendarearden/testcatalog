{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo879.serviceAccountName" . }}
  labels:
{{ include "foo879.labels" . | indent 4 }}
{{- end -}}
