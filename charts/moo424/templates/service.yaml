apiVersion: v1
kind: Service
metadata:
  name: {{ include "moo424.fullname" . }}
  labels:
{{ include "moo424.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "moo424.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
