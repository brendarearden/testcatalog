apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar610.fullname" . }}-test-connection"
  labels:
{{ include "bar610.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar610.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
