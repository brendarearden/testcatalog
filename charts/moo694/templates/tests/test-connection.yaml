apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo694.fullname" . }}-test-connection"
  labels:
{{ include "moo694.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo694.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
