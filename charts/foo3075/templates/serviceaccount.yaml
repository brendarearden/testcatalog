{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo3075.serviceAccountName" . }}
  labels:
{{ include "foo3075.labels" . | indent 4 }}
{{- end -}}
