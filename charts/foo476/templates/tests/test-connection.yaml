apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo476.fullname" . }}-test-connection"
  labels:
{{ include "foo476.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo476.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
