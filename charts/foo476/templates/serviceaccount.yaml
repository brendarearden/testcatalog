{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo476.serviceAccountName" . }}
  labels:
{{ include "foo476.labels" . | indent 4 }}
{{- end -}}
