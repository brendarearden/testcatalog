apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar716.fullname" . }}-test-connection"
  labels:
{{ include "bar716.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar716.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
