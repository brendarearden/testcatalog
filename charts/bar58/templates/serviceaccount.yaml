{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar58.serviceAccountName" . }}
  labels:
{{ include "bar58.labels" . | indent 4 }}
{{- end -}}
