{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo766.serviceAccountName" . }}
  labels:
{{ include "foo766.labels" . | indent 4 }}
{{- end -}}
