apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo766.fullname" . }}-test-connection"
  labels:
{{ include "foo766.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo766.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
