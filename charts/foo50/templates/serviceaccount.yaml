{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo50.serviceAccountName" . }}
  labels:
{{ include "foo50.labels" . | indent 4 }}
{{- end -}}
