{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar638.serviceAccountName" . }}
  labels:
{{ include "bar638.labels" . | indent 4 }}
{{- end -}}
