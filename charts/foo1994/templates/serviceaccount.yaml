{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1994.serviceAccountName" . }}
  labels:
{{ include "foo1994.labels" . | indent 4 }}
{{- end -}}
