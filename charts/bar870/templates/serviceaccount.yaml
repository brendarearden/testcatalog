{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar870.serviceAccountName" . }}
  labels:
{{ include "bar870.labels" . | indent 4 }}
{{- end -}}
