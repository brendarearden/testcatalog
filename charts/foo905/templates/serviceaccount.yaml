{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo905.serviceAccountName" . }}
  labels:
{{ include "foo905.labels" . | indent 4 }}
{{- end -}}
