{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo804.serviceAccountName" . }}
  labels:
{{ include "foo804.labels" . | indent 4 }}
{{- end -}}
