{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar699.serviceAccountName" . }}
  labels:
{{ include "bar699.labels" . | indent 4 }}
{{- end -}}
