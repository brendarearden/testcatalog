{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo287.serviceAccountName" . }}
  labels:
{{ include "moo287.labels" . | indent 4 }}
{{- end -}}
