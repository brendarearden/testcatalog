{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo164.serviceAccountName" . }}
  labels:
{{ include "foo164.labels" . | indent 4 }}
{{- end -}}
