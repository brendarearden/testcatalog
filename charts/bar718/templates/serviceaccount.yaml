{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar718.serviceAccountName" . }}
  labels:
{{ include "bar718.labels" . | indent 4 }}
{{- end -}}
