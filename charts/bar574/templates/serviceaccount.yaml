{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar574.serviceAccountName" . }}
  labels:
{{ include "bar574.labels" . | indent 4 }}
{{- end -}}
