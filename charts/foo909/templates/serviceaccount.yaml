{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo909.serviceAccountName" . }}
  labels:
{{ include "foo909.labels" . | indent 4 }}
{{- end -}}
