{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo3796.serviceAccountName" . }}
  labels:
{{ include "foo3796.labels" . | indent 4 }}
{{- end -}}
