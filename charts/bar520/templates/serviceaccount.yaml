{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar520.serviceAccountName" . }}
  labels:
{{ include "bar520.labels" . | indent 4 }}
{{- end -}}
