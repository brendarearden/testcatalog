{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar617.serviceAccountName" . }}
  labels:
{{ include "bar617.labels" . | indent 4 }}
{{- end -}}
