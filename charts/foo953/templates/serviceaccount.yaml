{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo953.serviceAccountName" . }}
  labels:
{{ include "foo953.labels" . | indent 4 }}
{{- end -}}
