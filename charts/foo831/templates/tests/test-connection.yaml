apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo831.fullname" . }}-test-connection"
  labels:
{{ include "foo831.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo831.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
