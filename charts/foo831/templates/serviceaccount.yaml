{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo831.serviceAccountName" . }}
  labels:
{{ include "foo831.labels" . | indent 4 }}
{{- end -}}
