{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1994.serviceAccountName" . }}
  labels:
{{ include "bar1994.labels" . | indent 4 }}
{{- end -}}
