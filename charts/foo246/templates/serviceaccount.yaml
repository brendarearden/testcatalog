{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo246.serviceAccountName" . }}
  labels:
{{ include "foo246.labels" . | indent 4 }}
{{- end -}}
