{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1567.serviceAccountName" . }}
  labels:
{{ include "bar1567.labels" . | indent 4 }}
{{- end -}}
