apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar87.fullname" . }}-test-connection"
  labels:
{{ include "bar87.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar87.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
