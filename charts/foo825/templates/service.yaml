apiVersion: v1
kind: Service
metadata:
  name: {{ include "foo825.fullname" . }}
  labels:
{{ include "foo825.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "foo825.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
