{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1255.serviceAccountName" . }}
  labels:
{{ include "bar1255.labels" . | indent 4 }}
{{- end -}}
