{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo651.serviceAccountName" . }}
  labels:
{{ include "foo651.labels" . | indent 4 }}
{{- end -}}
