{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo3006.serviceAccountName" . }}
  labels:
{{ include "foo3006.labels" . | indent 4 }}
{{- end -}}
