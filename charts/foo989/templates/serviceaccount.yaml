{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo989.serviceAccountName" . }}
  labels:
{{ include "foo989.labels" . | indent 4 }}
{{- end -}}
