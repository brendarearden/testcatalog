{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar484.serviceAccountName" . }}
  labels:
{{ include "bar484.labels" . | indent 4 }}
{{- end -}}
