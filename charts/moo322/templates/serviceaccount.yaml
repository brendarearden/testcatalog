{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo322.serviceAccountName" . }}
  labels:
{{ include "moo322.labels" . | indent 4 }}
{{- end -}}
