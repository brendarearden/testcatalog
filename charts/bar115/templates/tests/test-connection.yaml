apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar115.fullname" . }}-test-connection"
  labels:
{{ include "bar115.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar115.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
