{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar745.serviceAccountName" . }}
  labels:
{{ include "bar745.labels" . | indent 4 }}
{{- end -}}
