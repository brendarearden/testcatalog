apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo3981.fullname" . }}-test-connection"
  labels:
{{ include "foo3981.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo3981.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
