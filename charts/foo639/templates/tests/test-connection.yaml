apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo639.fullname" . }}-test-connection"
  labels:
{{ include "foo639.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo639.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
