{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo3133.serviceAccountName" . }}
  labels:
{{ include "foo3133.labels" . | indent 4 }}
{{- end -}}
