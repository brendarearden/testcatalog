{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo349.serviceAccountName" . }}
  labels:
{{ include "foo349.labels" . | indent 4 }}
{{- end -}}
