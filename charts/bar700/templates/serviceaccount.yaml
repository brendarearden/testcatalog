{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar700.serviceAccountName" . }}
  labels:
{{ include "bar700.labels" . | indent 4 }}
{{- end -}}
