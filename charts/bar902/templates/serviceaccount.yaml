{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar902.serviceAccountName" . }}
  labels:
{{ include "bar902.labels" . | indent 4 }}
{{- end -}}
