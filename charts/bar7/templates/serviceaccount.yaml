{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar7.serviceAccountName" . }}
  labels:
{{ include "bar7.labels" . | indent 4 }}
{{- end -}}
