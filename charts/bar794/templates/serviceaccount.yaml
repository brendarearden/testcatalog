{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar794.serviceAccountName" . }}
  labels:
{{ include "bar794.labels" . | indent 4 }}
{{- end -}}
