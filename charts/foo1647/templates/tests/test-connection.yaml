apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo1647.fullname" . }}-test-connection"
  labels:
{{ include "foo1647.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo1647.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
