apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo99.fullname" . }}-test-connection"
  labels:
{{ include "foo99.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo99.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
