{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1750.serviceAccountName" . }}
  labels:
{{ include "bar1750.labels" . | indent 4 }}
{{- end -}}
