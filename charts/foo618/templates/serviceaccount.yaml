{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo618.serviceAccountName" . }}
  labels:
{{ include "foo618.labels" . | indent 4 }}
{{- end -}}
