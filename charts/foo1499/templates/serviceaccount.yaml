{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1499.serviceAccountName" . }}
  labels:
{{ include "foo1499.labels" . | indent 4 }}
{{- end -}}
