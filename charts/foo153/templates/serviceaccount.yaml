{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo153.serviceAccountName" . }}
  labels:
{{ include "foo153.labels" . | indent 4 }}
{{- end -}}
