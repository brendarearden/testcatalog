apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo58.fullname" . }}-test-connection"
  labels:
{{ include "moo58.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo58.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
