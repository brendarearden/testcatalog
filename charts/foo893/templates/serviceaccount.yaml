{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo893.serviceAccountName" . }}
  labels:
{{ include "foo893.labels" . | indent 4 }}
{{- end -}}
