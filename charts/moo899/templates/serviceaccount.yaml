{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo899.serviceAccountName" . }}
  labels:
{{ include "moo899.labels" . | indent 4 }}
{{- end -}}
