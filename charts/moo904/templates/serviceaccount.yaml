{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo904.serviceAccountName" . }}
  labels:
{{ include "moo904.labels" . | indent 4 }}
{{- end -}}
