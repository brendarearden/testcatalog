{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar999.serviceAccountName" . }}
  labels:
{{ include "bar999.labels" . | indent 4 }}
{{- end -}}
