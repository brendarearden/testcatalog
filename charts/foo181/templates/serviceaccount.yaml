{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo181.serviceAccountName" . }}
  labels:
{{ include "foo181.labels" . | indent 4 }}
{{- end -}}
