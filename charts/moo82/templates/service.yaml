apiVersion: v1
kind: Service
metadata:
  name: {{ include "moo82.fullname" . }}
  labels:
{{ include "moo82.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "moo82.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
