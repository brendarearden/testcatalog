apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo739.fullname" . }}-test-connection"
  labels:
{{ include "foo739.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo739.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
