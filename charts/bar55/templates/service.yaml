apiVersion: v1
kind: Service
metadata:
  name: {{ include "bar55.fullname" . }}
  labels:
{{ include "bar55.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "bar55.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
