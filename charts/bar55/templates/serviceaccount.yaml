{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar55.serviceAccountName" . }}
  labels:
{{ include "bar55.labels" . | indent 4 }}
{{- end -}}
