{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo127.serviceAccountName" . }}
  labels:
{{ include "foo127.labels" . | indent 4 }}
{{- end -}}
