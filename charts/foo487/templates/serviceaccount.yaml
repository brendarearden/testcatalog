{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo487.serviceAccountName" . }}
  labels:
{{ include "foo487.labels" . | indent 4 }}
{{- end -}}
