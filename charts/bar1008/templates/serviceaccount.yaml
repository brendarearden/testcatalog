{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1008.serviceAccountName" . }}
  labels:
{{ include "bar1008.labels" . | indent 4 }}
{{- end -}}
