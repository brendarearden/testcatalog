apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo2115.fullname" . }}-test-connection"
  labels:
{{ include "moo2115.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo2115.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
