{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo503.serviceAccountName" . }}
  labels:
{{ include "foo503.labels" . | indent 4 }}
{{- end -}}
