{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar400.serviceAccountName" . }}
  labels:
{{ include "bar400.labels" . | indent 4 }}
{{- end -}}
