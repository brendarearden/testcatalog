{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1918.serviceAccountName" . }}
  labels:
{{ include "bar1918.labels" . | indent 4 }}
{{- end -}}
