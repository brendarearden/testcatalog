{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1962.serviceAccountName" . }}
  labels:
{{ include "bar1962.labels" . | indent 4 }}
{{- end -}}
