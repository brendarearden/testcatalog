{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar797.serviceAccountName" . }}
  labels:
{{ include "bar797.labels" . | indent 4 }}
{{- end -}}
