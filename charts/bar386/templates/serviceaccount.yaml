{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar386.serviceAccountName" . }}
  labels:
{{ include "bar386.labels" . | indent 4 }}
{{- end -}}
