{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo2127.serviceAccountName" . }}
  labels:
{{ include "foo2127.labels" . | indent 4 }}
{{- end -}}
