{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar360.serviceAccountName" . }}
  labels:
{{ include "bar360.labels" . | indent 4 }}
{{- end -}}
