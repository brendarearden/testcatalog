apiVersion: v1
kind: Service
metadata:
  name: {{ include "bar506.fullname" . }}
  labels:
{{ include "bar506.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "bar506.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
