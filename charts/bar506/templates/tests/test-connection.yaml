apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar506.fullname" . }}-test-connection"
  labels:
{{ include "bar506.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar506.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
