{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo620.serviceAccountName" . }}
  labels:
{{ include "moo620.labels" . | indent 4 }}
{{- end -}}
