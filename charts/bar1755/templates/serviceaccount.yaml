{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1755.serviceAccountName" . }}
  labels:
{{ include "bar1755.labels" . | indent 4 }}
{{- end -}}
