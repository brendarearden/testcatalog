{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo340.serviceAccountName" . }}
  labels:
{{ include "foo340.labels" . | indent 4 }}
{{- end -}}
