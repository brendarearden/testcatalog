{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar992.serviceAccountName" . }}
  labels:
{{ include "bar992.labels" . | indent 4 }}
{{- end -}}
