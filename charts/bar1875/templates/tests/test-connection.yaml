apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar1875.fullname" . }}-test-connection"
  labels:
{{ include "bar1875.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar1875.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
