{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1875.serviceAccountName" . }}
  labels:
{{ include "bar1875.labels" . | indent 4 }}
{{- end -}}
