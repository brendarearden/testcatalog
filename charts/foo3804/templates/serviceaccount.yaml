{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo3804.serviceAccountName" . }}
  labels:
{{ include "foo3804.labels" . | indent 4 }}
{{- end -}}
