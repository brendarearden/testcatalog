{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo3160.serviceAccountName" . }}
  labels:
{{ include "foo3160.labels" . | indent 4 }}
{{- end -}}
