{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo853.serviceAccountName" . }}
  labels:
{{ include "moo853.labels" . | indent 4 }}
{{- end -}}
