apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo853.fullname" . }}-test-connection"
  labels:
{{ include "moo853.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo853.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
