{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo469.serviceAccountName" . }}
  labels:
{{ include "foo469.labels" . | indent 4 }}
{{- end -}}
