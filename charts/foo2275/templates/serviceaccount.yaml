{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo2275.serviceAccountName" . }}
  labels:
{{ include "foo2275.labels" . | indent 4 }}
{{- end -}}
