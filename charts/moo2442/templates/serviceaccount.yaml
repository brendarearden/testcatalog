{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo2442.serviceAccountName" . }}
  labels:
{{ include "moo2442.labels" . | indent 4 }}
{{- end -}}
