{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo3656.serviceAccountName" . }}
  labels:
{{ include "foo3656.labels" . | indent 4 }}
{{- end -}}
