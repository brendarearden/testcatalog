{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1430.serviceAccountName" . }}
  labels:
{{ include "foo1430.labels" . | indent 4 }}
{{- end -}}
