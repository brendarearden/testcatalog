apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo729.fullname" . }}-test-connection"
  labels:
{{ include "moo729.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo729.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
