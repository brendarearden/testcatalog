{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo360.serviceAccountName" . }}
  labels:
{{ include "foo360.labels" . | indent 4 }}
{{- end -}}
