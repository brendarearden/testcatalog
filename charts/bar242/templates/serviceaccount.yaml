{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar242.serviceAccountName" . }}
  labels:
{{ include "bar242.labels" . | indent 4 }}
{{- end -}}
