{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo208.serviceAccountName" . }}
  labels:
{{ include "foo208.labels" . | indent 4 }}
{{- end -}}
