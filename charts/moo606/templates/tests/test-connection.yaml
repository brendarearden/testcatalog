apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo606.fullname" . }}-test-connection"
  labels:
{{ include "moo606.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo606.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
