{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1708.serviceAccountName" . }}
  labels:
{{ include "foo1708.labels" . | indent 4 }}
{{- end -}}
