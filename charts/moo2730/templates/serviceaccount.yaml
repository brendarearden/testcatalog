{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo2730.serviceAccountName" . }}
  labels:
{{ include "moo2730.labels" . | indent 4 }}
{{- end -}}
