{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo514.serviceAccountName" . }}
  labels:
{{ include "moo514.labels" . | indent 4 }}
{{- end -}}
