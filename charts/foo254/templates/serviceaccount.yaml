{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo254.serviceAccountName" . }}
  labels:
{{ include "foo254.labels" . | indent 4 }}
{{- end -}}
