{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar808.serviceAccountName" . }}
  labels:
{{ include "bar808.labels" . | indent 4 }}
{{- end -}}
