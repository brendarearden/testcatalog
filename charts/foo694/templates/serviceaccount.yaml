{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo694.serviceAccountName" . }}
  labels:
{{ include "foo694.labels" . | indent 4 }}
{{- end -}}
