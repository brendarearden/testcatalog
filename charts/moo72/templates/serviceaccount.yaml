{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo72.serviceAccountName" . }}
  labels:
{{ include "moo72.labels" . | indent 4 }}
{{- end -}}
