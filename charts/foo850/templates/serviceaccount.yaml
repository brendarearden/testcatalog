{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo850.serviceAccountName" . }}
  labels:
{{ include "foo850.labels" . | indent 4 }}
{{- end -}}
