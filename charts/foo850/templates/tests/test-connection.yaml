apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo850.fullname" . }}-test-connection"
  labels:
{{ include "foo850.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo850.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
