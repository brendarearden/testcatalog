{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo749.serviceAccountName" . }}
  labels:
{{ include "foo749.labels" . | indent 4 }}
{{- end -}}
