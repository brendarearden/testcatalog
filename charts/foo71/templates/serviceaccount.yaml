{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo71.serviceAccountName" . }}
  labels:
{{ include "foo71.labels" . | indent 4 }}
{{- end -}}
