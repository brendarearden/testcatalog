apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar435.fullname" . }}-test-connection"
  labels:
{{ include "bar435.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar435.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
