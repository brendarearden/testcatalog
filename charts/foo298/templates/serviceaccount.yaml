{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo298.serviceAccountName" . }}
  labels:
{{ include "foo298.labels" . | indent 4 }}
{{- end -}}
