{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo438.serviceAccountName" . }}
  labels:
{{ include "foo438.labels" . | indent 4 }}
{{- end -}}
