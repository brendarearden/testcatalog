{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar874.serviceAccountName" . }}
  labels:
{{ include "bar874.labels" . | indent 4 }}
{{- end -}}
