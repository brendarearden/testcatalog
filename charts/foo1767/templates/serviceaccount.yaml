{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1767.serviceAccountName" . }}
  labels:
{{ include "foo1767.labels" . | indent 4 }}
{{- end -}}
