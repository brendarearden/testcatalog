{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar707.serviceAccountName" . }}
  labels:
{{ include "bar707.labels" . | indent 4 }}
{{- end -}}
