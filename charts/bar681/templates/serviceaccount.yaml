{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar681.serviceAccountName" . }}
  labels:
{{ include "bar681.labels" . | indent 4 }}
{{- end -}}
