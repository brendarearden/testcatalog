{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar486.serviceAccountName" . }}
  labels:
{{ include "bar486.labels" . | indent 4 }}
{{- end -}}
