{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar244.serviceAccountName" . }}
  labels:
{{ include "bar244.labels" . | indent 4 }}
{{- end -}}
