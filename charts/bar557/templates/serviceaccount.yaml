{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar557.serviceAccountName" . }}
  labels:
{{ include "bar557.labels" . | indent 4 }}
{{- end -}}
