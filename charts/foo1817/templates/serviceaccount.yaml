{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1817.serviceAccountName" . }}
  labels:
{{ include "foo1817.labels" . | indent 4 }}
{{- end -}}
