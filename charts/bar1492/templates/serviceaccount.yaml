{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1492.serviceAccountName" . }}
  labels:
{{ include "bar1492.labels" . | indent 4 }}
{{- end -}}
