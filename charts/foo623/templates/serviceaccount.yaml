{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo623.serviceAccountName" . }}
  labels:
{{ include "foo623.labels" . | indent 4 }}
{{- end -}}
