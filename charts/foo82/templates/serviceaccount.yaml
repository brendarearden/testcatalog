{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo82.serviceAccountName" . }}
  labels:
{{ include "foo82.labels" . | indent 4 }}
{{- end -}}
