{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo634.serviceAccountName" . }}
  labels:
{{ include "foo634.labels" . | indent 4 }}
{{- end -}}
