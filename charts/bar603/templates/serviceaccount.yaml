{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar603.serviceAccountName" . }}
  labels:
{{ include "bar603.labels" . | indent 4 }}
{{- end -}}
