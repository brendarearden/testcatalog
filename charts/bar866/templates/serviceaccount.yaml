{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar866.serviceAccountName" . }}
  labels:
{{ include "bar866.labels" . | indent 4 }}
{{- end -}}
