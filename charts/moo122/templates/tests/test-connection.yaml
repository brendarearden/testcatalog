apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo122.fullname" . }}-test-connection"
  labels:
{{ include "moo122.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo122.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
