{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo555.serviceAccountName" . }}
  labels:
{{ include "moo555.labels" . | indent 4 }}
{{- end -}}
