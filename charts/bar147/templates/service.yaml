apiVersion: v1
kind: Service
metadata:
  name: {{ include "bar147.fullname" . }}
  labels:
{{ include "bar147.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "bar147.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
