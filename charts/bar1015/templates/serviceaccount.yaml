{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1015.serviceAccountName" . }}
  labels:
{{ include "bar1015.labels" . | indent 4 }}
{{- end -}}
