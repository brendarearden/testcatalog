apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo886.fullname" . }}-test-connection"
  labels:
{{ include "moo886.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo886.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
