apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar23.fullname" . }}-test-connection"
  labels:
{{ include "bar23.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar23.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
