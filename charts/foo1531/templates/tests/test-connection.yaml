apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo1531.fullname" . }}-test-connection"
  labels:
{{ include "foo1531.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo1531.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
