{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo952.serviceAccountName" . }}
  labels:
{{ include "foo952.labels" . | indent 4 }}
{{- end -}}
