apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo1362.fullname" . }}-test-connection"
  labels:
{{ include "moo1362.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo1362.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
