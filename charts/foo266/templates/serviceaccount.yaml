{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo266.serviceAccountName" . }}
  labels:
{{ include "foo266.labels" . | indent 4 }}
{{- end -}}
