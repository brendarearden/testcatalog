{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar168.serviceAccountName" . }}
  labels:
{{ include "bar168.labels" . | indent 4 }}
{{- end -}}
