{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo204.serviceAccountName" . }}
  labels:
{{ include "moo204.labels" . | indent 4 }}
{{- end -}}
