{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo811.serviceAccountName" . }}
  labels:
{{ include "foo811.labels" . | indent 4 }}
{{- end -}}
