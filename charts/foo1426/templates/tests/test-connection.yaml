apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo1426.fullname" . }}-test-connection"
  labels:
{{ include "foo1426.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo1426.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
