apiVersion: v1
kind: Service
metadata:
  name: {{ include "moo100.fullname" . }}
  labels:
{{ include "moo100.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "moo100.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
