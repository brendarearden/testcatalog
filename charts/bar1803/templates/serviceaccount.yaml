{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1803.serviceAccountName" . }}
  labels:
{{ include "bar1803.labels" . | indent 4 }}
{{- end -}}
