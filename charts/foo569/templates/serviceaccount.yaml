{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo569.serviceAccountName" . }}
  labels:
{{ include "foo569.labels" . | indent 4 }}
{{- end -}}
