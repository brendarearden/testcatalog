apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo709.fullname" . }}-test-connection"
  labels:
{{ include "foo709.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo709.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
