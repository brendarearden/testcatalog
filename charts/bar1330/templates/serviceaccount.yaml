{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1330.serviceAccountName" . }}
  labels:
{{ include "bar1330.labels" . | indent 4 }}
{{- end -}}
