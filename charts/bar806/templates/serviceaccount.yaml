{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar806.serviceAccountName" . }}
  labels:
{{ include "bar806.labels" . | indent 4 }}
{{- end -}}
