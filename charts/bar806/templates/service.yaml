apiVersion: v1
kind: Service
metadata:
  name: {{ include "bar806.fullname" . }}
  labels:
{{ include "bar806.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "bar806.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
