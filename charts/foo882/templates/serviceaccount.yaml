{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo882.serviceAccountName" . }}
  labels:
{{ include "foo882.labels" . | indent 4 }}
{{- end -}}
