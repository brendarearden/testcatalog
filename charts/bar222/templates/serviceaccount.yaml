{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar222.serviceAccountName" . }}
  labels:
{{ include "bar222.labels" . | indent 4 }}
{{- end -}}
