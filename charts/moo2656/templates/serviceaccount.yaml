{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo2656.serviceAccountName" . }}
  labels:
{{ include "moo2656.labels" . | indent 4 }}
{{- end -}}
