{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1638.serviceAccountName" . }}
  labels:
{{ include "foo1638.labels" . | indent 4 }}
{{- end -}}
