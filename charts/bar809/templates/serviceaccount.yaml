{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar809.serviceAccountName" . }}
  labels:
{{ include "bar809.labels" . | indent 4 }}
{{- end -}}
