{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar886.serviceAccountName" . }}
  labels:
{{ include "bar886.labels" . | indent 4 }}
{{- end -}}
