{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo898.serviceAccountName" . }}
  labels:
{{ include "foo898.labels" . | indent 4 }}
{{- end -}}
