{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar286.serviceAccountName" . }}
  labels:
{{ include "bar286.labels" . | indent 4 }}
{{- end -}}
