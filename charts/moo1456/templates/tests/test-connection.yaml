apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo1456.fullname" . }}-test-connection"
  labels:
{{ include "moo1456.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo1456.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
