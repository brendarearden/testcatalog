{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo789.serviceAccountName" . }}
  labels:
{{ include "foo789.labels" . | indent 4 }}
{{- end -}}
