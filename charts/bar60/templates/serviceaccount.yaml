{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar60.serviceAccountName" . }}
  labels:
{{ include "bar60.labels" . | indent 4 }}
{{- end -}}
