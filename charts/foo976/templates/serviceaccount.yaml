{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo976.serviceAccountName" . }}
  labels:
{{ include "foo976.labels" . | indent 4 }}
{{- end -}}
