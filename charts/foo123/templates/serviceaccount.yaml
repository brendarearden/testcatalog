{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo123.serviceAccountName" . }}
  labels:
{{ include "foo123.labels" . | indent 4 }}
{{- end -}}
