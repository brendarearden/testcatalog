{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo385.serviceAccountName" . }}
  labels:
{{ include "foo385.labels" . | indent 4 }}
{{- end -}}
