apiVersion: v1
kind: Service
metadata:
  name: {{ include "moo609.fullname" . }}
  labels:
{{ include "moo609.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "moo609.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
