{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar948.serviceAccountName" . }}
  labels:
{{ include "bar948.labels" . | indent 4 }}
{{- end -}}
