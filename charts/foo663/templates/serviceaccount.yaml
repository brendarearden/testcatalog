{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo663.serviceAccountName" . }}
  labels:
{{ include "foo663.labels" . | indent 4 }}
{{- end -}}
