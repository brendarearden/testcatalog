{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo2266.serviceAccountName" . }}
  labels:
{{ include "moo2266.labels" . | indent 4 }}
{{- end -}}
