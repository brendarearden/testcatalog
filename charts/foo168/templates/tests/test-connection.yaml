apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo168.fullname" . }}-test-connection"
  labels:
{{ include "foo168.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo168.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
