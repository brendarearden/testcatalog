{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo201.serviceAccountName" . }}
  labels:
{{ include "foo201.labels" . | indent 4 }}
{{- end -}}
