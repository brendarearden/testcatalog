{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo39.serviceAccountName" . }}
  labels:
{{ include "foo39.labels" . | indent 4 }}
{{- end -}}
