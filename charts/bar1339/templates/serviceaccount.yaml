{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1339.serviceAccountName" . }}
  labels:
{{ include "bar1339.labels" . | indent 4 }}
{{- end -}}
