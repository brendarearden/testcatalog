{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar266.serviceAccountName" . }}
  labels:
{{ include "bar266.labels" . | indent 4 }}
{{- end -}}
