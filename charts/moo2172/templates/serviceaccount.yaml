{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo2172.serviceAccountName" . }}
  labels:
{{ include "moo2172.labels" . | indent 4 }}
{{- end -}}
