apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo703.fullname" . }}-test-connection"
  labels:
{{ include "moo703.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo703.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
