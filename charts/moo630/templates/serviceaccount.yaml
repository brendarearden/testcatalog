{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo630.serviceAccountName" . }}
  labels:
{{ include "moo630.labels" . | indent 4 }}
{{- end -}}
