{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar130.serviceAccountName" . }}
  labels:
{{ include "bar130.labels" . | indent 4 }}
{{- end -}}
