apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo383.fullname" . }}-test-connection"
  labels:
{{ include "foo383.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo383.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
