{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1303.serviceAccountName" . }}
  labels:
{{ include "bar1303.labels" . | indent 4 }}
{{- end -}}
