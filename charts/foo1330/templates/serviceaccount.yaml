{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1330.serviceAccountName" . }}
  labels:
{{ include "foo1330.labels" . | indent 4 }}
{{- end -}}
