{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar451.serviceAccountName" . }}
  labels:
{{ include "bar451.labels" . | indent 4 }}
{{- end -}}
