{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo2664.serviceAccountName" . }}
  labels:
{{ include "moo2664.labels" . | indent 4 }}
{{- end -}}
