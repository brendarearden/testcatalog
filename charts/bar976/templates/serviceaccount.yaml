{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar976.serviceAccountName" . }}
  labels:
{{ include "bar976.labels" . | indent 4 }}
{{- end -}}
