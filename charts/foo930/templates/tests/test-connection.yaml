apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo930.fullname" . }}-test-connection"
  labels:
{{ include "foo930.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo930.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
