apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar1545.fullname" . }}-test-connection"
  labels:
{{ include "bar1545.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar1545.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
