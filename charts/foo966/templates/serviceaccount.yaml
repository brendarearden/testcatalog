{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo966.serviceAccountName" . }}
  labels:
{{ include "foo966.labels" . | indent 4 }}
{{- end -}}
