{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo3775.serviceAccountName" . }}
  labels:
{{ include "foo3775.labels" . | indent 4 }}
{{- end -}}
