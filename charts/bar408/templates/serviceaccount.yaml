{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar408.serviceAccountName" . }}
  labels:
{{ include "bar408.labels" . | indent 4 }}
{{- end -}}
