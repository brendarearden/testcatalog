{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1517.serviceAccountName" . }}
  labels:
{{ include "bar1517.labels" . | indent 4 }}
{{- end -}}
