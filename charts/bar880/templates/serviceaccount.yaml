{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar880.serviceAccountName" . }}
  labels:
{{ include "bar880.labels" . | indent 4 }}
{{- end -}}
