{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo547.serviceAccountName" . }}
  labels:
{{ include "moo547.labels" . | indent 4 }}
{{- end -}}
