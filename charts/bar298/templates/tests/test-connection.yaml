apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar298.fullname" . }}-test-connection"
  labels:
{{ include "bar298.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar298.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
