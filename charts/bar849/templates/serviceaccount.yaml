{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar849.serviceAccountName" . }}
  labels:
{{ include "bar849.labels" . | indent 4 }}
{{- end -}}
