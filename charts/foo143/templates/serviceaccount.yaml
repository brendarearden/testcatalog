{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo143.serviceAccountName" . }}
  labels:
{{ include "foo143.labels" . | indent 4 }}
{{- end -}}
