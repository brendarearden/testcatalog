{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar678.serviceAccountName" . }}
  labels:
{{ include "bar678.labels" . | indent 4 }}
{{- end -}}
