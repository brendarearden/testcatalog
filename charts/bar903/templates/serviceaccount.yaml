{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar903.serviceAccountName" . }}
  labels:
{{ include "bar903.labels" . | indent 4 }}
{{- end -}}
