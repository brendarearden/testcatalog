{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar280.serviceAccountName" . }}
  labels:
{{ include "bar280.labels" . | indent 4 }}
{{- end -}}
