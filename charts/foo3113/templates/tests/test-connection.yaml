apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo3113.fullname" . }}-test-connection"
  labels:
{{ include "foo3113.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo3113.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
