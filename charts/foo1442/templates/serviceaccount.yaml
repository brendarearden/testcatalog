{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1442.serviceAccountName" . }}
  labels:
{{ include "foo1442.labels" . | indent 4 }}
{{- end -}}
