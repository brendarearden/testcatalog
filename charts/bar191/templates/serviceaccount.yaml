{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar191.serviceAccountName" . }}
  labels:
{{ include "bar191.labels" . | indent 4 }}
{{- end -}}
