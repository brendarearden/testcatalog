{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar496.serviceAccountName" . }}
  labels:
{{ include "bar496.labels" . | indent 4 }}
{{- end -}}
