{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo185.serviceAccountName" . }}
  labels:
{{ include "foo185.labels" . | indent 4 }}
{{- end -}}
