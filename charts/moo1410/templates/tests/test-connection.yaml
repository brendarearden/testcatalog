apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo1410.fullname" . }}-test-connection"
  labels:
{{ include "moo1410.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo1410.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
