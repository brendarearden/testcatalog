{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo720.serviceAccountName" . }}
  labels:
{{ include "foo720.labels" . | indent 4 }}
{{- end -}}
