{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1993.serviceAccountName" . }}
  labels:
{{ include "bar1993.labels" . | indent 4 }}
{{- end -}}
