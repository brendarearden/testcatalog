{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo643.serviceAccountName" . }}
  labels:
{{ include "foo643.labels" . | indent 4 }}
{{- end -}}
