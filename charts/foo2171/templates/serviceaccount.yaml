{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo2171.serviceAccountName" . }}
  labels:
{{ include "foo2171.labels" . | indent 4 }}
{{- end -}}
