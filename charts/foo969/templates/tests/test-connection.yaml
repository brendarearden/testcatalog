apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo969.fullname" . }}-test-connection"
  labels:
{{ include "foo969.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo969.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
