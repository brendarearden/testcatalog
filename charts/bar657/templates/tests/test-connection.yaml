apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar657.fullname" . }}-test-connection"
  labels:
{{ include "bar657.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar657.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
