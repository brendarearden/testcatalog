{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo583.serviceAccountName" . }}
  labels:
{{ include "foo583.labels" . | indent 4 }}
{{- end -}}
