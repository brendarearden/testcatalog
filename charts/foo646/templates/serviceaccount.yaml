{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo646.serviceAccountName" . }}
  labels:
{{ include "foo646.labels" . | indent 4 }}
{{- end -}}
