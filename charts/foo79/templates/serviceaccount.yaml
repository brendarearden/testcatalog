{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo79.serviceAccountName" . }}
  labels:
{{ include "foo79.labels" . | indent 4 }}
{{- end -}}
