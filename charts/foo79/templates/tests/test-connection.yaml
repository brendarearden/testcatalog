apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo79.fullname" . }}-test-connection"
  labels:
{{ include "foo79.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo79.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
