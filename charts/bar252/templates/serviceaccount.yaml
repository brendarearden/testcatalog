{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar252.serviceAccountName" . }}
  labels:
{{ include "bar252.labels" . | indent 4 }}
{{- end -}}
