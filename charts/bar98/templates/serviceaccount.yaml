{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar98.serviceAccountName" . }}
  labels:
{{ include "bar98.labels" . | indent 4 }}
{{- end -}}
