{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1411.serviceAccountName" . }}
  labels:
{{ include "bar1411.labels" . | indent 4 }}
{{- end -}}
