apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar1831.fullname" . }}-test-connection"
  labels:
{{ include "bar1831.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar1831.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
