{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo629.serviceAccountName" . }}
  labels:
{{ include "foo629.labels" . | indent 4 }}
{{- end -}}
