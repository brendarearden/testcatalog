{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo49.serviceAccountName" . }}
  labels:
{{ include "foo49.labels" . | indent 4 }}
{{- end -}}
