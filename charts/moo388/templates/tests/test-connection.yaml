apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo388.fullname" . }}-test-connection"
  labels:
{{ include "moo388.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo388.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
