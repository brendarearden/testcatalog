apiVersion: v1
kind: Service
metadata:
  name: {{ include "bar724.fullname" . }}
  labels:
{{ include "bar724.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "bar724.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
