{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar431.serviceAccountName" . }}
  labels:
{{ include "bar431.labels" . | indent 4 }}
{{- end -}}
