apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo780.fullname" . }}-test-connection"
  labels:
{{ include "foo780.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo780.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
