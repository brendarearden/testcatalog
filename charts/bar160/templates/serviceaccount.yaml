{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar160.serviceAccountName" . }}
  labels:
{{ include "bar160.labels" . | indent 4 }}
{{- end -}}
