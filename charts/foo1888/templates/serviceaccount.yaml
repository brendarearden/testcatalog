{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1888.serviceAccountName" . }}
  labels:
{{ include "foo1888.labels" . | indent 4 }}
{{- end -}}
