{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo704.serviceAccountName" . }}
  labels:
{{ include "foo704.labels" . | indent 4 }}
{{- end -}}
