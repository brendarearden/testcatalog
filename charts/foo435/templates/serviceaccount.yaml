{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo435.serviceAccountName" . }}
  labels:
{{ include "foo435.labels" . | indent 4 }}
{{- end -}}
