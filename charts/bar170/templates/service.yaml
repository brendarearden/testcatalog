apiVersion: v1
kind: Service
metadata:
  name: {{ include "bar170.fullname" . }}
  labels:
{{ include "bar170.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "bar170.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
