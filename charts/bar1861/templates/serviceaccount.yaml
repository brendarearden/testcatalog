{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1861.serviceAccountName" . }}
  labels:
{{ include "bar1861.labels" . | indent 4 }}
{{- end -}}
