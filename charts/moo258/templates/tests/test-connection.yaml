apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo258.fullname" . }}-test-connection"
  labels:
{{ include "moo258.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo258.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
