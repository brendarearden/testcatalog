{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar67.serviceAccountName" . }}
  labels:
{{ include "bar67.labels" . | indent 4 }}
{{- end -}}
