{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar982.serviceAccountName" . }}
  labels:
{{ include "bar982.labels" . | indent 4 }}
{{- end -}}
