{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo57.serviceAccountName" . }}
  labels:
{{ include "foo57.labels" . | indent 4 }}
{{- end -}}
