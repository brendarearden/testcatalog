{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1522.serviceAccountName" . }}
  labels:
{{ include "foo1522.labels" . | indent 4 }}
{{- end -}}
