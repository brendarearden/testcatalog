{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar376.serviceAccountName" . }}
  labels:
{{ include "bar376.labels" . | indent 4 }}
{{- end -}}
