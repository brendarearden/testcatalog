{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo3985.serviceAccountName" . }}
  labels:
{{ include "foo3985.labels" . | indent 4 }}
{{- end -}}
