{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo3459.serviceAccountName" . }}
  labels:
{{ include "foo3459.labels" . | indent 4 }}
{{- end -}}
