{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo787.serviceAccountName" . }}
  labels:
{{ include "moo787.labels" . | indent 4 }}
{{- end -}}
