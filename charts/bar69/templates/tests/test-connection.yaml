apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar69.fullname" . }}-test-connection"
  labels:
{{ include "bar69.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar69.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
