apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo1363.fullname" . }}-test-connection"
  labels:
{{ include "foo1363.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo1363.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
