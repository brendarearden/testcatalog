apiVersion: v1
kind: Service
metadata:
  name: {{ include "bar838.fullname" . }}
  labels:
{{ include "bar838.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "bar838.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
