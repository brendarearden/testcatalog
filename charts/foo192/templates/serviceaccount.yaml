{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo192.serviceAccountName" . }}
  labels:
{{ include "foo192.labels" . | indent 4 }}
{{- end -}}
