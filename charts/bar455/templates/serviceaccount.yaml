{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar455.serviceAccountName" . }}
  labels:
{{ include "bar455.labels" . | indent 4 }}
{{- end -}}
