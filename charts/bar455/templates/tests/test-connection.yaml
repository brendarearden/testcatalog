apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar455.fullname" . }}-test-connection"
  labels:
{{ include "bar455.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar455.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
