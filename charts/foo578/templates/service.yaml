apiVersion: v1
kind: Service
metadata:
  name: {{ include "foo578.fullname" . }}
  labels:
{{ include "foo578.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "foo578.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
