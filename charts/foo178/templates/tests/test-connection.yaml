apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo178.fullname" . }}-test-connection"
  labels:
{{ include "foo178.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo178.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
