{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar477.serviceAccountName" . }}
  labels:
{{ include "bar477.labels" . | indent 4 }}
{{- end -}}
