apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo266.fullname" . }}-test-connection"
  labels:
{{ include "moo266.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo266.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
