{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo266.serviceAccountName" . }}
  labels:
{{ include "moo266.labels" . | indent 4 }}
{{- end -}}
