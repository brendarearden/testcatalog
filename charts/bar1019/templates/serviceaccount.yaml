{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1019.serviceAccountName" . }}
  labels:
{{ include "bar1019.labels" . | indent 4 }}
{{- end -}}
