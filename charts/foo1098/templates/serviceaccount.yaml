{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1098.serviceAccountName" . }}
  labels:
{{ include "foo1098.labels" . | indent 4 }}
{{- end -}}
