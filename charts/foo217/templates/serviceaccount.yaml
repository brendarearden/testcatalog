{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo217.serviceAccountName" . }}
  labels:
{{ include "foo217.labels" . | indent 4 }}
{{- end -}}
