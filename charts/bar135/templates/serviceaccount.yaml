{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar135.serviceAccountName" . }}
  labels:
{{ include "bar135.labels" . | indent 4 }}
{{- end -}}
