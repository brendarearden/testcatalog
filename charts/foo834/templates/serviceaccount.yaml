{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo834.serviceAccountName" . }}
  labels:
{{ include "foo834.labels" . | indent 4 }}
{{- end -}}
