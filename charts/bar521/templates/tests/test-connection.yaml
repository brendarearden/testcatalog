apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar521.fullname" . }}-test-connection"
  labels:
{{ include "bar521.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar521.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
