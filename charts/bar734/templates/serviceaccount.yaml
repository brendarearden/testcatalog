{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar734.serviceAccountName" . }}
  labels:
{{ include "bar734.labels" . | indent 4 }}
{{- end -}}
