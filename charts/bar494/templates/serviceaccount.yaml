{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar494.serviceAccountName" . }}
  labels:
{{ include "bar494.labels" . | indent 4 }}
{{- end -}}
