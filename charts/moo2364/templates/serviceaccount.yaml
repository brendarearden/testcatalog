{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo2364.serviceAccountName" . }}
  labels:
{{ include "moo2364.labels" . | indent 4 }}
{{- end -}}
