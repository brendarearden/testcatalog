{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo738.serviceAccountName" . }}
  labels:
{{ include "foo738.labels" . | indent 4 }}
{{- end -}}
