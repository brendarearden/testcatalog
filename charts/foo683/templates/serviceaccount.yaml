{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo683.serviceAccountName" . }}
  labels:
{{ include "foo683.labels" . | indent 4 }}
{{- end -}}
