apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar1533.fullname" . }}-test-connection"
  labels:
{{ include "bar1533.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar1533.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
