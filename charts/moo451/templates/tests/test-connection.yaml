apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo451.fullname" . }}-test-connection"
  labels:
{{ include "moo451.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo451.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
