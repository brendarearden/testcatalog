{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar708.serviceAccountName" . }}
  labels:
{{ include "bar708.labels" . | indent 4 }}
{{- end -}}
