{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar907.serviceAccountName" . }}
  labels:
{{ include "bar907.labels" . | indent 4 }}
{{- end -}}
