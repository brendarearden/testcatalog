{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar388.serviceAccountName" . }}
  labels:
{{ include "bar388.labels" . | indent 4 }}
{{- end -}}
