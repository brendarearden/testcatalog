{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1412.serviceAccountName" . }}
  labels:
{{ include "foo1412.labels" . | indent 4 }}
{{- end -}}
