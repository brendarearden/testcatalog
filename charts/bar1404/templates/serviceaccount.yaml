{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1404.serviceAccountName" . }}
  labels:
{{ include "bar1404.labels" . | indent 4 }}
{{- end -}}
