{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar644.serviceAccountName" . }}
  labels:
{{ include "bar644.labels" . | indent 4 }}
{{- end -}}
