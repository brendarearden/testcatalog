{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo543.serviceAccountName" . }}
  labels:
{{ include "foo543.labels" . | indent 4 }}
{{- end -}}
