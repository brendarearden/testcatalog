{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1399.serviceAccountName" . }}
  labels:
{{ include "bar1399.labels" . | indent 4 }}
{{- end -}}
