apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo933.fullname" . }}-test-connection"
  labels:
{{ include "foo933.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo933.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
