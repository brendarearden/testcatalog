{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo933.serviceAccountName" . }}
  labels:
{{ include "foo933.labels" . | indent 4 }}
{{- end -}}
