{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar174.serviceAccountName" . }}
  labels:
{{ include "bar174.labels" . | indent 4 }}
{{- end -}}
