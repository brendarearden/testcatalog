{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo65.serviceAccountName" . }}
  labels:
{{ include "foo65.labels" . | indent 4 }}
{{- end -}}
