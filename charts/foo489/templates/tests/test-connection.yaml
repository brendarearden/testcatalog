apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo489.fullname" . }}-test-connection"
  labels:
{{ include "foo489.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo489.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
