{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar636.serviceAccountName" . }}
  labels:
{{ include "bar636.labels" . | indent 4 }}
{{- end -}}
