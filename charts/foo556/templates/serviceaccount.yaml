{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo556.serviceAccountName" . }}
  labels:
{{ include "foo556.labels" . | indent 4 }}
{{- end -}}
