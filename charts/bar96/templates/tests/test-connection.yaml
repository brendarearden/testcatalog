apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar96.fullname" . }}-test-connection"
  labels:
{{ include "bar96.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar96.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
