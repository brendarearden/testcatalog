{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar6.serviceAccountName" . }}
  labels:
{{ include "bar6.labels" . | indent 4 }}
{{- end -}}
