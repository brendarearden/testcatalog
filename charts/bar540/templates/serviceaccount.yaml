{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar540.serviceAccountName" . }}
  labels:
{{ include "bar540.labels" . | indent 4 }}
{{- end -}}
