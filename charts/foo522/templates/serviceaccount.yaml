{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo522.serviceAccountName" . }}
  labels:
{{ include "foo522.labels" . | indent 4 }}
{{- end -}}
