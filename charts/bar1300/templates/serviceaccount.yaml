{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1300.serviceAccountName" . }}
  labels:
{{ include "bar1300.labels" . | indent 4 }}
{{- end -}}
