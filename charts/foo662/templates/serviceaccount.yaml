{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo662.serviceAccountName" . }}
  labels:
{{ include "foo662.labels" . | indent 4 }}
{{- end -}}
