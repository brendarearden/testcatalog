apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar424.fullname" . }}-test-connection"
  labels:
{{ include "bar424.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar424.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
