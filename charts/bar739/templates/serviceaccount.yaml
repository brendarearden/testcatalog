{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar739.serviceAccountName" . }}
  labels:
{{ include "bar739.labels" . | indent 4 }}
{{- end -}}
