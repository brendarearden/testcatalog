apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar777.fullname" . }}-test-connection"
  labels:
{{ include "bar777.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar777.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
