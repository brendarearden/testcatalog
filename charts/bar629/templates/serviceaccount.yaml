{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar629.serviceAccountName" . }}
  labels:
{{ include "bar629.labels" . | indent 4 }}
{{- end -}}
