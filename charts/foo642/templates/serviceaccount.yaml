{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo642.serviceAccountName" . }}
  labels:
{{ include "foo642.labels" . | indent 4 }}
{{- end -}}
