{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar549.serviceAccountName" . }}
  labels:
{{ include "bar549.labels" . | indent 4 }}
{{- end -}}
