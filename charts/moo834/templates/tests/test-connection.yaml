apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo834.fullname" . }}-test-connection"
  labels:
{{ include "moo834.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo834.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
