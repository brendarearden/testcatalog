{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar503.serviceAccountName" . }}
  labels:
{{ include "bar503.labels" . | indent 4 }}
{{- end -}}
