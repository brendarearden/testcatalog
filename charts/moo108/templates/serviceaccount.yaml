{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo108.serviceAccountName" . }}
  labels:
{{ include "moo108.labels" . | indent 4 }}
{{- end -}}
