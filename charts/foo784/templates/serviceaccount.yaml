{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo784.serviceAccountName" . }}
  labels:
{{ include "foo784.labels" . | indent 4 }}
{{- end -}}
