{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar40.serviceAccountName" . }}
  labels:
{{ include "bar40.labels" . | indent 4 }}
{{- end -}}
