{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar299.serviceAccountName" . }}
  labels:
{{ include "bar299.labels" . | indent 4 }}
{{- end -}}
