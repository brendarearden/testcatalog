{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1170.serviceAccountName" . }}
  labels:
{{ include "foo1170.labels" . | indent 4 }}
{{- end -}}
