apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo222.fullname" . }}-test-connection"
  labels:
{{ include "foo222.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo222.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
