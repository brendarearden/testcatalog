{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo317.serviceAccountName" . }}
  labels:
{{ include "foo317.labels" . | indent 4 }}
{{- end -}}
