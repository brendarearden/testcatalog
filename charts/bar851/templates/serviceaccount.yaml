{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar851.serviceAccountName" . }}
  labels:
{{ include "bar851.labels" . | indent 4 }}
{{- end -}}
