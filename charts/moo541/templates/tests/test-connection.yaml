apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo541.fullname" . }}-test-connection"
  labels:
{{ include "moo541.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo541.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
