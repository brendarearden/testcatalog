{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo846.serviceAccountName" . }}
  labels:
{{ include "foo846.labels" . | indent 4 }}
{{- end -}}
