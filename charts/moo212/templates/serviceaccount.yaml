{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo212.serviceAccountName" . }}
  labels:
{{ include "moo212.labels" . | indent 4 }}
{{- end -}}
