apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar445.fullname" . }}-test-connection"
  labels:
{{ include "bar445.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar445.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
