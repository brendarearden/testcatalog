{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar190.serviceAccountName" . }}
  labels:
{{ include "bar190.labels" . | indent 4 }}
{{- end -}}
