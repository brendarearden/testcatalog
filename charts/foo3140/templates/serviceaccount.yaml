{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo3140.serviceAccountName" . }}
  labels:
{{ include "foo3140.labels" . | indent 4 }}
{{- end -}}
