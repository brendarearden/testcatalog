{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar390.serviceAccountName" . }}
  labels:
{{ include "bar390.labels" . | indent 4 }}
{{- end -}}
