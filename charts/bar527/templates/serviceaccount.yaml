{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar527.serviceAccountName" . }}
  labels:
{{ include "bar527.labels" . | indent 4 }}
{{- end -}}
