{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1195.serviceAccountName" . }}
  labels:
{{ include "foo1195.labels" . | indent 4 }}
{{- end -}}
