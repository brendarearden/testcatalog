{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar513.serviceAccountName" . }}
  labels:
{{ include "bar513.labels" . | indent 4 }}
{{- end -}}
