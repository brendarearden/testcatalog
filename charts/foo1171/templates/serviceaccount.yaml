{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1171.serviceAccountName" . }}
  labels:
{{ include "foo1171.labels" . | indent 4 }}
{{- end -}}
