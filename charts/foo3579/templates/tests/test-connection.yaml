apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo3579.fullname" . }}-test-connection"
  labels:
{{ include "foo3579.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo3579.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
