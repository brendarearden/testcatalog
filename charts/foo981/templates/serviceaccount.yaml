{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo981.serviceAccountName" . }}
  labels:
{{ include "foo981.labels" . | indent 4 }}
{{- end -}}
