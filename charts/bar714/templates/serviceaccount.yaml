{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar714.serviceAccountName" . }}
  labels:
{{ include "bar714.labels" . | indent 4 }}
{{- end -}}
