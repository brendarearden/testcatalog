{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo457.serviceAccountName" . }}
  labels:
{{ include "foo457.labels" . | indent 4 }}
{{- end -}}
