{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo981.serviceAccountName" . }}
  labels:
{{ include "moo981.labels" . | indent 4 }}
{{- end -}}
