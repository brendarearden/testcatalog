apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo164.fullname" . }}-test-connection"
  labels:
{{ include "moo164.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo164.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
