{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo2088.serviceAccountName" . }}
  labels:
{{ include "foo2088.labels" . | indent 4 }}
{{- end -}}
