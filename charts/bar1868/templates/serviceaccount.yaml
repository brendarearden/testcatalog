{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1868.serviceAccountName" . }}
  labels:
{{ include "bar1868.labels" . | indent 4 }}
{{- end -}}
