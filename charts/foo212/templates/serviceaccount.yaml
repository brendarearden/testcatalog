{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo212.serviceAccountName" . }}
  labels:
{{ include "foo212.labels" . | indent 4 }}
{{- end -}}
