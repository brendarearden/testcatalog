apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo1013.fullname" . }}-test-connection"
  labels:
{{ include "moo1013.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo1013.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
