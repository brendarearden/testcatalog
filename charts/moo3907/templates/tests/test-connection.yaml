apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo3907.fullname" . }}-test-connection"
  labels:
{{ include "moo3907.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo3907.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
