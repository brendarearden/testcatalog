{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo920.serviceAccountName" . }}
  labels:
{{ include "foo920.labels" . | indent 4 }}
{{- end -}}
