{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1732.serviceAccountName" . }}
  labels:
{{ include "bar1732.labels" . | indent 4 }}
{{- end -}}
