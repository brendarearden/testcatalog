apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo3406.fullname" . }}-test-connection"
  labels:
{{ include "foo3406.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo3406.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
