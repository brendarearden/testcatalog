{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1717.serviceAccountName" . }}
  labels:
{{ include "foo1717.labels" . | indent 4 }}
{{- end -}}
