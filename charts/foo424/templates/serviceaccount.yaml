{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo424.serviceAccountName" . }}
  labels:
{{ include "foo424.labels" . | indent 4 }}
{{- end -}}
