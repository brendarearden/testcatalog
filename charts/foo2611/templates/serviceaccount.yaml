{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo2611.serviceAccountName" . }}
  labels:
{{ include "foo2611.labels" . | indent 4 }}
{{- end -}}
