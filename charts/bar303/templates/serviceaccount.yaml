{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar303.serviceAccountName" . }}
  labels:
{{ include "bar303.labels" . | indent 4 }}
{{- end -}}
