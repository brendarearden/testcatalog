apiVersion: v1
kind: Service
metadata:
  name: {{ include "foo791.fullname" . }}
  labels:
{{ include "foo791.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "foo791.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
