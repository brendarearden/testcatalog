{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo474.serviceAccountName" . }}
  labels:
{{ include "foo474.labels" . | indent 4 }}
{{- end -}}
