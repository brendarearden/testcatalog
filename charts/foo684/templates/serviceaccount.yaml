{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo684.serviceAccountName" . }}
  labels:
{{ include "foo684.labels" . | indent 4 }}
{{- end -}}
