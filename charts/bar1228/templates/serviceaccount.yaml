{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1228.serviceAccountName" . }}
  labels:
{{ include "bar1228.labels" . | indent 4 }}
{{- end -}}
