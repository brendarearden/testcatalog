{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1400.serviceAccountName" . }}
  labels:
{{ include "bar1400.labels" . | indent 4 }}
{{- end -}}
