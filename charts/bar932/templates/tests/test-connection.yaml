apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar932.fullname" . }}-test-connection"
  labels:
{{ include "bar932.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar932.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
