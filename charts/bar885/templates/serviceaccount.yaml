{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar885.serviceAccountName" . }}
  labels:
{{ include "bar885.labels" . | indent 4 }}
{{- end -}}
