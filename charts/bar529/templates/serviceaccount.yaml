{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar529.serviceAccountName" . }}
  labels:
{{ include "bar529.labels" . | indent 4 }}
{{- end -}}
