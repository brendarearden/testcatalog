{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo44.serviceAccountName" . }}
  labels:
{{ include "foo44.labels" . | indent 4 }}
{{- end -}}
