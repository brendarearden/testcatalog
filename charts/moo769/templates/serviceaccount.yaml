{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo769.serviceAccountName" . }}
  labels:
{{ include "moo769.labels" . | indent 4 }}
{{- end -}}
