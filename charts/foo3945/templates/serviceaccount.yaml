{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo3945.serviceAccountName" . }}
  labels:
{{ include "foo3945.labels" . | indent 4 }}
{{- end -}}
