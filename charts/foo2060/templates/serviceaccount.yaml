{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo2060.serviceAccountName" . }}
  labels:
{{ include "foo2060.labels" . | indent 4 }}
{{- end -}}
