{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo308.serviceAccountName" . }}
  labels:
{{ include "foo308.labels" . | indent 4 }}
{{- end -}}
