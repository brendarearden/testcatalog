{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1034.serviceAccountName" . }}
  labels:
{{ include "foo1034.labels" . | indent 4 }}
{{- end -}}
