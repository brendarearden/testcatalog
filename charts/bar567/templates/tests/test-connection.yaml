apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar567.fullname" . }}-test-connection"
  labels:
{{ include "bar567.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar567.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
