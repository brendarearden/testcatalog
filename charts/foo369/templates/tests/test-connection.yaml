apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo369.fullname" . }}-test-connection"
  labels:
{{ include "foo369.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo369.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
