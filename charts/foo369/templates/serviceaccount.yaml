{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo369.serviceAccountName" . }}
  labels:
{{ include "foo369.labels" . | indent 4 }}
{{- end -}}
