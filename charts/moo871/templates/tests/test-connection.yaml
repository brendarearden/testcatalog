apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo871.fullname" . }}-test-connection"
  labels:
{{ include "moo871.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo871.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
