{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo871.serviceAccountName" . }}
  labels:
{{ include "moo871.labels" . | indent 4 }}
{{- end -}}
