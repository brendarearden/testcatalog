{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar879.serviceAccountName" . }}
  labels:
{{ include "bar879.labels" . | indent 4 }}
{{- end -}}
