apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo1965.fullname" . }}-test-connection"
  labels:
{{ include "foo1965.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo1965.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
