{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar139.serviceAccountName" . }}
  labels:
{{ include "bar139.labels" . | indent 4 }}
{{- end -}}
