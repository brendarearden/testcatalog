{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar72.serviceAccountName" . }}
  labels:
{{ include "bar72.labels" . | indent 4 }}
{{- end -}}
