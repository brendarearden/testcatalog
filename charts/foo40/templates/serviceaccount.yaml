{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo40.serviceAccountName" . }}
  labels:
{{ include "foo40.labels" . | indent 4 }}
{{- end -}}
