apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo558.fullname" . }}-test-connection"
  labels:
{{ include "moo558.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo558.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
