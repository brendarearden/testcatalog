{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo412.serviceAccountName" . }}
  labels:
{{ include "foo412.labels" . | indent 4 }}
{{- end -}}
