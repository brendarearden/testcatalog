{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo964.serviceAccountName" . }}
  labels:
{{ include "moo964.labels" . | indent 4 }}
{{- end -}}
