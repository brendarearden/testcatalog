apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo2006.fullname" . }}-test-connection"
  labels:
{{ include "foo2006.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo2006.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
