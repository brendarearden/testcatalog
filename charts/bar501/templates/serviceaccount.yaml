{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar501.serviceAccountName" . }}
  labels:
{{ include "bar501.labels" . | indent 4 }}
{{- end -}}
