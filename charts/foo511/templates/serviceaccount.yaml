{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo511.serviceAccountName" . }}
  labels:
{{ include "foo511.labels" . | indent 4 }}
{{- end -}}
