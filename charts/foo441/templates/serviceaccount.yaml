{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo441.serviceAccountName" . }}
  labels:
{{ include "foo441.labels" . | indent 4 }}
{{- end -}}
