apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo955.fullname" . }}-test-connection"
  labels:
{{ include "foo955.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo955.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
