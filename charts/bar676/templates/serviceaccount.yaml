{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar676.serviceAccountName" . }}
  labels:
{{ include "bar676.labels" . | indent 4 }}
{{- end -}}
