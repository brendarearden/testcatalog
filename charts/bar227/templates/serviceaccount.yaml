{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar227.serviceAccountName" . }}
  labels:
{{ include "bar227.labels" . | indent 4 }}
{{- end -}}
