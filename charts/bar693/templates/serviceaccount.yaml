{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar693.serviceAccountName" . }}
  labels:
{{ include "bar693.labels" . | indent 4 }}
{{- end -}}
