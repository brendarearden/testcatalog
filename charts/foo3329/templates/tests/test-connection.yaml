apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo3329.fullname" . }}-test-connection"
  labels:
{{ include "foo3329.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo3329.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
