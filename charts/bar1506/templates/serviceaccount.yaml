{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1506.serviceAccountName" . }}
  labels:
{{ include "bar1506.labels" . | indent 4 }}
{{- end -}}
