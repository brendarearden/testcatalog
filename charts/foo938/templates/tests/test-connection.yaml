apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo938.fullname" . }}-test-connection"
  labels:
{{ include "foo938.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo938.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
