{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo772.serviceAccountName" . }}
  labels:
{{ include "foo772.labels" . | indent 4 }}
{{- end -}}
