apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo924.fullname" . }}-test-connection"
  labels:
{{ include "moo924.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo924.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
