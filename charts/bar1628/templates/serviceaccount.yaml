{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1628.serviceAccountName" . }}
  labels:
{{ include "bar1628.labels" . | indent 4 }}
{{- end -}}
