{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar112.serviceAccountName" . }}
  labels:
{{ include "bar112.labels" . | indent 4 }}
{{- end -}}
