{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar109.serviceAccountName" . }}
  labels:
{{ include "bar109.labels" . | indent 4 }}
{{- end -}}
