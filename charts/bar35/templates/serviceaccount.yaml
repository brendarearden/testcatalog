{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar35.serviceAccountName" . }}
  labels:
{{ include "bar35.labels" . | indent 4 }}
{{- end -}}
