{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar634.serviceAccountName" . }}
  labels:
{{ include "bar634.labels" . | indent 4 }}
{{- end -}}
