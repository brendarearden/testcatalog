{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo858.serviceAccountName" . }}
  labels:
{{ include "moo858.labels" . | indent 4 }}
{{- end -}}
