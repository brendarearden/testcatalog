{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1359.serviceAccountName" . }}
  labels:
{{ include "bar1359.labels" . | indent 4 }}
{{- end -}}
