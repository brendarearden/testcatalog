{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo245.serviceAccountName" . }}
  labels:
{{ include "foo245.labels" . | indent 4 }}
{{- end -}}
