{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1804.serviceAccountName" . }}
  labels:
{{ include "foo1804.labels" . | indent 4 }}
{{- end -}}
