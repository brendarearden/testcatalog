{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar614.serviceAccountName" . }}
  labels:
{{ include "bar614.labels" . | indent 4 }}
{{- end -}}
