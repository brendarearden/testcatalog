{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1413.serviceAccountName" . }}
  labels:
{{ include "foo1413.labels" . | indent 4 }}
{{- end -}}
