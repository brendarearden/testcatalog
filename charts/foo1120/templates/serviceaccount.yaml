{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1120.serviceAccountName" . }}
  labels:
{{ include "foo1120.labels" . | indent 4 }}
{{- end -}}
