{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo450.serviceAccountName" . }}
  labels:
{{ include "foo450.labels" . | indent 4 }}
{{- end -}}
