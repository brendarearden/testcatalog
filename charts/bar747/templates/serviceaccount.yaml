{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar747.serviceAccountName" . }}
  labels:
{{ include "bar747.labels" . | indent 4 }}
{{- end -}}
