{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo3720.serviceAccountName" . }}
  labels:
{{ include "foo3720.labels" . | indent 4 }}
{{- end -}}
