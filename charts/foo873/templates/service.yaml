apiVersion: v1
kind: Service
metadata:
  name: {{ include "foo873.fullname" . }}
  labels:
{{ include "foo873.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "foo873.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
