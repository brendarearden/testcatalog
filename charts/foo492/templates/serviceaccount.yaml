{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo492.serviceAccountName" . }}
  labels:
{{ include "foo492.labels" . | indent 4 }}
{{- end -}}
