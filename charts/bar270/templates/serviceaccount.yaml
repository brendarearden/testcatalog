{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar270.serviceAccountName" . }}
  labels:
{{ include "bar270.labels" . | indent 4 }}
{{- end -}}
