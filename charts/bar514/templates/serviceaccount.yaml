{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar514.serviceAccountName" . }}
  labels:
{{ include "bar514.labels" . | indent 4 }}
{{- end -}}
