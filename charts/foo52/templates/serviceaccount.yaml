{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo52.serviceAccountName" . }}
  labels:
{{ include "foo52.labels" . | indent 4 }}
{{- end -}}
