{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar310.serviceAccountName" . }}
  labels:
{{ include "bar310.labels" . | indent 4 }}
{{- end -}}
