{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo64.serviceAccountName" . }}
  labels:
{{ include "foo64.labels" . | indent 4 }}
{{- end -}}
