{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar972.serviceAccountName" . }}
  labels:
{{ include "bar972.labels" . | indent 4 }}
{{- end -}}
