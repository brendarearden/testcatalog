{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar287.serviceAccountName" . }}
  labels:
{{ include "bar287.labels" . | indent 4 }}
{{- end -}}
