{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar524.serviceAccountName" . }}
  labels:
{{ include "bar524.labels" . | indent 4 }}
{{- end -}}
