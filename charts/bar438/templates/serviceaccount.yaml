{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar438.serviceAccountName" . }}
  labels:
{{ include "bar438.labels" . | indent 4 }}
{{- end -}}
