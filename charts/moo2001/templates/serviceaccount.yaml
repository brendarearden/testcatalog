{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo2001.serviceAccountName" . }}
  labels:
{{ include "moo2001.labels" . | indent 4 }}
{{- end -}}
