{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1968.serviceAccountName" . }}
  labels:
{{ include "bar1968.labels" . | indent 4 }}
{{- end -}}
