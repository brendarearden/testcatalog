apiVersion: v1
kind: Service
metadata:
  name: {{ include "bar1968.fullname" . }}
  labels:
{{ include "bar1968.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "bar1968.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
