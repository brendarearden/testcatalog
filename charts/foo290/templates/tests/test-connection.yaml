apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo290.fullname" . }}-test-connection"
  labels:
{{ include "foo290.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo290.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
