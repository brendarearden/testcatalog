{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1185.serviceAccountName" . }}
  labels:
{{ include "foo1185.labels" . | indent 4 }}
{{- end -}}
