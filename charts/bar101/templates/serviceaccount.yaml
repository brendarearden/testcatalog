{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar101.serviceAccountName" . }}
  labels:
{{ include "bar101.labels" . | indent 4 }}
{{- end -}}
