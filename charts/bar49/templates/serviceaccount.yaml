{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar49.serviceAccountName" . }}
  labels:
{{ include "bar49.labels" . | indent 4 }}
{{- end -}}
