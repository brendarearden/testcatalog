{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo95.serviceAccountName" . }}
  labels:
{{ include "foo95.labels" . | indent 4 }}
{{- end -}}
