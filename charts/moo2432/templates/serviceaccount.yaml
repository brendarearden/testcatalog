{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo2432.serviceAccountName" . }}
  labels:
{{ include "moo2432.labels" . | indent 4 }}
{{- end -}}
