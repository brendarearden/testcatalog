{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo752.serviceAccountName" . }}
  labels:
{{ include "foo752.labels" . | indent 4 }}
{{- end -}}
