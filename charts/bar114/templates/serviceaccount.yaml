{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar114.serviceAccountName" . }}
  labels:
{{ include "bar114.labels" . | indent 4 }}
{{- end -}}
