{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo493.serviceAccountName" . }}
  labels:
{{ include "foo493.labels" . | indent 4 }}
{{- end -}}
