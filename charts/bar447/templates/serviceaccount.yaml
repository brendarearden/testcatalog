{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar447.serviceAccountName" . }}
  labels:
{{ include "bar447.labels" . | indent 4 }}
{{- end -}}
