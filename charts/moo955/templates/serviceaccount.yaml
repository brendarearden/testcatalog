{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo955.serviceAccountName" . }}
  labels:
{{ include "moo955.labels" . | indent 4 }}
{{- end -}}
