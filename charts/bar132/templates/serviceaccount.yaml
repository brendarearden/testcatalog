{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar132.serviceAccountName" . }}
  labels:
{{ include "bar132.labels" . | indent 4 }}
{{- end -}}
