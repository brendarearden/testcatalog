apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo185.fullname" . }}-test-connection"
  labels:
{{ include "moo185.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo185.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
