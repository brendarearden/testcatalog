{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1304.serviceAccountName" . }}
  labels:
{{ include "bar1304.labels" . | indent 4 }}
{{- end -}}
