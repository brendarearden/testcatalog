{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo628.serviceAccountName" . }}
  labels:
{{ include "foo628.labels" . | indent 4 }}
{{- end -}}
