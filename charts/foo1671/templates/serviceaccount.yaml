{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1671.serviceAccountName" . }}
  labels:
{{ include "foo1671.labels" . | indent 4 }}
{{- end -}}
