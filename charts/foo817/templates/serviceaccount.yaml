{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo817.serviceAccountName" . }}
  labels:
{{ include "foo817.labels" . | indent 4 }}
{{- end -}}
