{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1588.serviceAccountName" . }}
  labels:
{{ include "bar1588.labels" . | indent 4 }}
{{- end -}}
