apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo3465.fullname" . }}-test-connection"
  labels:
{{ include "foo3465.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo3465.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
