{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo875.serviceAccountName" . }}
  labels:
{{ include "foo875.labels" . | indent 4 }}
{{- end -}}
