{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1073.serviceAccountName" . }}
  labels:
{{ include "bar1073.labels" . | indent 4 }}
{{- end -}}
