{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar538.serviceAccountName" . }}
  labels:
{{ include "bar538.labels" . | indent 4 }}
{{- end -}}
