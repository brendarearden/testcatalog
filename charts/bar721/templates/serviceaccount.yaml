{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar721.serviceAccountName" . }}
  labels:
{{ include "bar721.labels" . | indent 4 }}
{{- end -}}
