{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar867.serviceAccountName" . }}
  labels:
{{ include "bar867.labels" . | indent 4 }}
{{- end -}}
