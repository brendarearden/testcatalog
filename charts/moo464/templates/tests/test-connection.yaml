apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo464.fullname" . }}-test-connection"
  labels:
{{ include "moo464.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo464.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
