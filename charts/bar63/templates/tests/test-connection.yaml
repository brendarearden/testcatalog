apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar63.fullname" . }}-test-connection"
  labels:
{{ include "bar63.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar63.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
