{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo715.serviceAccountName" . }}
  labels:
{{ include "foo715.labels" . | indent 4 }}
{{- end -}}
