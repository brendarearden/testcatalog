{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1664.serviceAccountName" . }}
  labels:
{{ include "foo1664.labels" . | indent 4 }}
{{- end -}}
