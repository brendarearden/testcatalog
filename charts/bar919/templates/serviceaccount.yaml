{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar919.serviceAccountName" . }}
  labels:
{{ include "bar919.labels" . | indent 4 }}
{{- end -}}
