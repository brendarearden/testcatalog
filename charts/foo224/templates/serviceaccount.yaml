{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo224.serviceAccountName" . }}
  labels:
{{ include "foo224.labels" . | indent 4 }}
{{- end -}}
