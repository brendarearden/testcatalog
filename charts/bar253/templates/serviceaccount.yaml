{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar253.serviceAccountName" . }}
  labels:
{{ include "bar253.labels" . | indent 4 }}
{{- end -}}
