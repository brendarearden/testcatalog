{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1172.serviceAccountName" . }}
  labels:
{{ include "bar1172.labels" . | indent 4 }}
{{- end -}}
