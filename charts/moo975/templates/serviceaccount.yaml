{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo975.serviceAccountName" . }}
  labels:
{{ include "moo975.labels" . | indent 4 }}
{{- end -}}
