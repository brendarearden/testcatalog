{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo599.serviceAccountName" . }}
  labels:
{{ include "moo599.labels" . | indent 4 }}
{{- end -}}
