apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo250.fullname" . }}-test-connection"
  labels:
{{ include "moo250.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo250.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
