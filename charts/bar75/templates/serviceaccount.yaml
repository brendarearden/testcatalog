{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar75.serviceAccountName" . }}
  labels:
{{ include "bar75.labels" . | indent 4 }}
{{- end -}}
