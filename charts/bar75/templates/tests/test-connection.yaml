apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar75.fullname" . }}-test-connection"
  labels:
{{ include "bar75.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar75.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
