{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo2754.serviceAccountName" . }}
  labels:
{{ include "foo2754.labels" . | indent 4 }}
{{- end -}}
