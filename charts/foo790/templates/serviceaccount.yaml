{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo790.serviceAccountName" . }}
  labels:
{{ include "foo790.labels" . | indent 4 }}
{{- end -}}
