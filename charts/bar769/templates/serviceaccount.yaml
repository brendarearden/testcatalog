{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar769.serviceAccountName" . }}
  labels:
{{ include "bar769.labels" . | indent 4 }}
{{- end -}}
