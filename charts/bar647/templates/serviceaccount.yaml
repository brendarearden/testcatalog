{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar647.serviceAccountName" . }}
  labels:
{{ include "bar647.labels" . | indent 4 }}
{{- end -}}
