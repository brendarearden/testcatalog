{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar814.serviceAccountName" . }}
  labels:
{{ include "bar814.labels" . | indent 4 }}
{{- end -}}
