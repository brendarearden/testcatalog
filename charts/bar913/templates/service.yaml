apiVersion: v1
kind: Service
metadata:
  name: {{ include "bar913.fullname" . }}
  labels:
{{ include "bar913.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "bar913.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
