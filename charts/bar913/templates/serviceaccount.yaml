{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar913.serviceAccountName" . }}
  labels:
{{ include "bar913.labels" . | indent 4 }}
{{- end -}}
