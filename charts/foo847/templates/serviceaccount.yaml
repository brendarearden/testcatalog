{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo847.serviceAccountName" . }}
  labels:
{{ include "foo847.labels" . | indent 4 }}
{{- end -}}
