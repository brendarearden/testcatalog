{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1310.serviceAccountName" . }}
  labels:
{{ include "bar1310.labels" . | indent 4 }}
{{- end -}}
