apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo1887.fullname" . }}-test-connection"
  labels:
{{ include "foo1887.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo1887.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
