{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1674.serviceAccountName" . }}
  labels:
{{ include "bar1674.labels" . | indent 4 }}
{{- end -}}
