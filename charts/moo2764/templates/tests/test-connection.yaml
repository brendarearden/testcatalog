apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo2764.fullname" . }}-test-connection"
  labels:
{{ include "moo2764.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo2764.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
