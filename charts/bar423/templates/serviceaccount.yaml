{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar423.serviceAccountName" . }}
  labels:
{{ include "bar423.labels" . | indent 4 }}
{{- end -}}
