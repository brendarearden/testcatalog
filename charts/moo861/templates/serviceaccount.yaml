{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo861.serviceAccountName" . }}
  labels:
{{ include "moo861.labels" . | indent 4 }}
{{- end -}}
