{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo356.serviceAccountName" . }}
  labels:
{{ include "foo356.labels" . | indent 4 }}
{{- end -}}
