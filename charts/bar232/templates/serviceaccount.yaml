{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar232.serviceAccountName" . }}
  labels:
{{ include "bar232.labels" . | indent 4 }}
{{- end -}}
