apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo3699.fullname" . }}-test-connection"
  labels:
{{ include "foo3699.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo3699.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
