{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo3037.serviceAccountName" . }}
  labels:
{{ include "foo3037.labels" . | indent 4 }}
{{- end -}}
