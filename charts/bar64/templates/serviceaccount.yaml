{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar64.serviceAccountName" . }}
  labels:
{{ include "bar64.labels" . | indent 4 }}
{{- end -}}
