{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar77.serviceAccountName" . }}
  labels:
{{ include "bar77.labels" . | indent 4 }}
{{- end -}}
