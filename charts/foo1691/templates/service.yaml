apiVersion: v1
kind: Service
metadata:
  name: {{ include "foo1691.fullname" . }}
  labels:
{{ include "foo1691.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "foo1691.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
