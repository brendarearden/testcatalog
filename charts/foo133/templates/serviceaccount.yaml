{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo133.serviceAccountName" . }}
  labels:
{{ include "foo133.labels" . | indent 4 }}
{{- end -}}
