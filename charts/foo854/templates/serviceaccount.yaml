{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo854.serviceAccountName" . }}
  labels:
{{ include "foo854.labels" . | indent 4 }}
{{- end -}}
