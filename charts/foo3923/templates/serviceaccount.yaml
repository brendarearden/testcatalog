{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo3923.serviceAccountName" . }}
  labels:
{{ include "foo3923.labels" . | indent 4 }}
{{- end -}}
