{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo2803.serviceAccountName" . }}
  labels:
{{ include "moo2803.labels" . | indent 4 }}
{{- end -}}
