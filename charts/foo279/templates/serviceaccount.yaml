{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo279.serviceAccountName" . }}
  labels:
{{ include "foo279.labels" . | indent 4 }}
{{- end -}}
