{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo221.serviceAccountName" . }}
  labels:
{{ include "foo221.labels" . | indent 4 }}
{{- end -}}
