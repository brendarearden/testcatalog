{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1224.serviceAccountName" . }}
  labels:
{{ include "bar1224.labels" . | indent 4 }}
{{- end -}}
