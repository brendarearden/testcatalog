{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar234.serviceAccountName" . }}
  labels:
{{ include "bar234.labels" . | indent 4 }}
{{- end -}}
