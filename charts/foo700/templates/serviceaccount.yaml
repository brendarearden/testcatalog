{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo700.serviceAccountName" . }}
  labels:
{{ include "foo700.labels" . | indent 4 }}
{{- end -}}
