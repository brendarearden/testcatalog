apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo339.fullname" . }}-test-connection"
  labels:
{{ include "moo339.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo339.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
