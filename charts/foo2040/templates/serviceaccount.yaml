{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo2040.serviceAccountName" . }}
  labels:
{{ include "foo2040.labels" . | indent 4 }}
{{- end -}}
