{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1431.serviceAccountName" . }}
  labels:
{{ include "foo1431.labels" . | indent 4 }}
{{- end -}}
