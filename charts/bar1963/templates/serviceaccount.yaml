{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1963.serviceAccountName" . }}
  labels:
{{ include "bar1963.labels" . | indent 4 }}
{{- end -}}
