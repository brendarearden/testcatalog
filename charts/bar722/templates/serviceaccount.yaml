{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar722.serviceAccountName" . }}
  labels:
{{ include "bar722.labels" . | indent 4 }}
{{- end -}}
