apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo409.fullname" . }}-test-connection"
  labels:
{{ include "foo409.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo409.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
