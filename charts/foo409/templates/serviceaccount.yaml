{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo409.serviceAccountName" . }}
  labels:
{{ include "foo409.labels" . | indent 4 }}
{{- end -}}
