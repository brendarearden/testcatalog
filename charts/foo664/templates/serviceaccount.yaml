{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo664.serviceAccountName" . }}
  labels:
{{ include "foo664.labels" . | indent 4 }}
{{- end -}}
