{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo504.serviceAccountName" . }}
  labels:
{{ include "foo504.labels" . | indent 4 }}
{{- end -}}
