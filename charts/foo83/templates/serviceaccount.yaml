{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo83.serviceAccountName" . }}
  labels:
{{ include "foo83.labels" . | indent 4 }}
{{- end -}}
