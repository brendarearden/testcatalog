{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo651.serviceAccountName" . }}
  labels:
{{ include "moo651.labels" . | indent 4 }}
{{- end -}}
