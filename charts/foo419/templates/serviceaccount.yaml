{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo419.serviceAccountName" . }}
  labels:
{{ include "foo419.labels" . | indent 4 }}
{{- end -}}
