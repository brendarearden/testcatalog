apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo1726.fullname" . }}-test-connection"
  labels:
{{ include "moo1726.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo1726.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
