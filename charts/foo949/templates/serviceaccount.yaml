{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo949.serviceAccountName" . }}
  labels:
{{ include "foo949.labels" . | indent 4 }}
{{- end -}}
