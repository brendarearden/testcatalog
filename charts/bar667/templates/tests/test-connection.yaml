apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar667.fullname" . }}-test-connection"
  labels:
{{ include "bar667.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar667.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
