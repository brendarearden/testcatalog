{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar623.serviceAccountName" . }}
  labels:
{{ include "bar623.labels" . | indent 4 }}
{{- end -}}
