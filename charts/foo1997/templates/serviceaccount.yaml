{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1997.serviceAccountName" . }}
  labels:
{{ include "foo1997.labels" . | indent 4 }}
{{- end -}}
