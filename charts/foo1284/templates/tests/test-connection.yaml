apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo1284.fullname" . }}-test-connection"
  labels:
{{ include "foo1284.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo1284.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
