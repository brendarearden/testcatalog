{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar701.serviceAccountName" . }}
  labels:
{{ include "bar701.labels" . | indent 4 }}
{{- end -}}
