{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar76.serviceAccountName" . }}
  labels:
{{ include "bar76.labels" . | indent 4 }}
{{- end -}}
