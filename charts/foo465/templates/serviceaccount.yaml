{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo465.serviceAccountName" . }}
  labels:
{{ include "foo465.labels" . | indent 4 }}
{{- end -}}
