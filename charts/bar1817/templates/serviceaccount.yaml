{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1817.serviceAccountName" . }}
  labels:
{{ include "bar1817.labels" . | indent 4 }}
{{- end -}}
