{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo411.serviceAccountName" . }}
  labels:
{{ include "foo411.labels" . | indent 4 }}
{{- end -}}
