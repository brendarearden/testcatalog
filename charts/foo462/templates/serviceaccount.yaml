{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo462.serviceAccountName" . }}
  labels:
{{ include "foo462.labels" . | indent 4 }}
{{- end -}}
