{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo473.serviceAccountName" . }}
  labels:
{{ include "foo473.labels" . | indent 4 }}
{{- end -}}
