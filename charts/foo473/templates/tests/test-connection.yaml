apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo473.fullname" . }}-test-connection"
  labels:
{{ include "foo473.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo473.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
