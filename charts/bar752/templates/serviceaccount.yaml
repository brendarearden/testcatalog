{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar752.serviceAccountName" . }}
  labels:
{{ include "bar752.labels" . | indent 4 }}
{{- end -}}
