apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar43.fullname" . }}-test-connection"
  labels:
{{ include "bar43.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar43.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
