{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar43.serviceAccountName" . }}
  labels:
{{ include "bar43.labels" . | indent 4 }}
{{- end -}}
