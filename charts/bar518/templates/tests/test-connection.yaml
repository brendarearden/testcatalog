apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar518.fullname" . }}-test-connection"
  labels:
{{ include "bar518.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar518.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
