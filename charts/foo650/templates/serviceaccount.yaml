{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo650.serviceAccountName" . }}
  labels:
{{ include "foo650.labels" . | indent 4 }}
{{- end -}}
