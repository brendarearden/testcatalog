{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar706.serviceAccountName" . }}
  labels:
{{ include "bar706.labels" . | indent 4 }}
{{- end -}}
