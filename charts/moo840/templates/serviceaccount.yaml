{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo840.serviceAccountName" . }}
  labels:
{{ include "moo840.labels" . | indent 4 }}
{{- end -}}
