{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar210.serviceAccountName" . }}
  labels:
{{ include "bar210.labels" . | indent 4 }}
{{- end -}}
