{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo625.serviceAccountName" . }}
  labels:
{{ include "foo625.labels" . | indent 4 }}
{{- end -}}
