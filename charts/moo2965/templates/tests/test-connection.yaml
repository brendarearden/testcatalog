apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo2965.fullname" . }}-test-connection"
  labels:
{{ include "moo2965.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo2965.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
