apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo52.fullname" . }}-test-connection"
  labels:
{{ include "moo52.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo52.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
