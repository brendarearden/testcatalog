apiVersion: v1
kind: Service
metadata:
  name: {{ include "foo3296.fullname" . }}
  labels:
{{ include "foo3296.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "foo3296.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
