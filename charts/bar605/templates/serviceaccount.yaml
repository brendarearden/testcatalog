{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar605.serviceAccountName" . }}
  labels:
{{ include "bar605.labels" . | indent 4 }}
{{- end -}}
