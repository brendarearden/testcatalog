{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar468.serviceAccountName" . }}
  labels:
{{ include "bar468.labels" . | indent 4 }}
{{- end -}}
