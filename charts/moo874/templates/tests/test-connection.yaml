apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo874.fullname" . }}-test-connection"
  labels:
{{ include "moo874.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo874.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
