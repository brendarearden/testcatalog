{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1562.serviceAccountName" . }}
  labels:
{{ include "foo1562.labels" . | indent 4 }}
{{- end -}}
