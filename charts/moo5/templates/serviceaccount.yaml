{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo5.serviceAccountName" . }}
  labels:
{{ include "moo5.labels" . | indent 4 }}
{{- end -}}
