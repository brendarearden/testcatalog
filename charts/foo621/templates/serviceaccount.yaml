{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo621.serviceAccountName" . }}
  labels:
{{ include "foo621.labels" . | indent 4 }}
{{- end -}}
