{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo1120.serviceAccountName" . }}
  labels:
{{ include "moo1120.labels" . | indent 4 }}
{{- end -}}
