{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar787.serviceAccountName" . }}
  labels:
{{ include "bar787.labels" . | indent 4 }}
{{- end -}}
