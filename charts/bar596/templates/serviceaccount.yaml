{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar596.serviceAccountName" . }}
  labels:
{{ include "bar596.labels" . | indent 4 }}
{{- end -}}
