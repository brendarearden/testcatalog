{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar446.serviceAccountName" . }}
  labels:
{{ include "bar446.labels" . | indent 4 }}
{{- end -}}
