{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo518.serviceAccountName" . }}
  labels:
{{ include "foo518.labels" . | indent 4 }}
{{- end -}}
