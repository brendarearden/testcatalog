apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo1384.fullname" . }}-test-connection"
  labels:
{{ include "moo1384.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo1384.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
