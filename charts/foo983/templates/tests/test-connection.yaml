apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo983.fullname" . }}-test-connection"
  labels:
{{ include "foo983.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo983.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
