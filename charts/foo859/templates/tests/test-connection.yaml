apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo859.fullname" . }}-test-connection"
  labels:
{{ include "foo859.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo859.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
