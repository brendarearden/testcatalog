apiVersion: v1
kind: Service
metadata:
  name: {{ include "foo859.fullname" . }}
  labels:
{{ include "foo859.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "foo859.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
