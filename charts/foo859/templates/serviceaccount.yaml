{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo859.serviceAccountName" . }}
  labels:
{{ include "foo859.labels" . | indent 4 }}
{{- end -}}
