{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1213.serviceAccountName" . }}
  labels:
{{ include "bar1213.labels" . | indent 4 }}
{{- end -}}
