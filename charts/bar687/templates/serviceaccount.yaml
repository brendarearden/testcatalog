{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar687.serviceAccountName" . }}
  labels:
{{ include "bar687.labels" . | indent 4 }}
{{- end -}}
