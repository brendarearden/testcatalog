apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo833.fullname" . }}-test-connection"
  labels:
{{ include "foo833.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo833.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
