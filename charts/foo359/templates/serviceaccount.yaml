{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo359.serviceAccountName" . }}
  labels:
{{ include "foo359.labels" . | indent 4 }}
{{- end -}}
