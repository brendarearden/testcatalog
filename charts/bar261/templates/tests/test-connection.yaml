apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar261.fullname" . }}-test-connection"
  labels:
{{ include "bar261.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar261.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
