apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo361.fullname" . }}-test-connection"
  labels:
{{ include "foo361.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo361.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
