apiVersion: v1
kind: Service
metadata:
  name: {{ include "moo3810.fullname" . }}
  labels:
{{ include "moo3810.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "moo3810.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
