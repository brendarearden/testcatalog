apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar726.fullname" . }}-test-connection"
  labels:
{{ include "bar726.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar726.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
