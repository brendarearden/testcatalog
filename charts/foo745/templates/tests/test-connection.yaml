apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo745.fullname" . }}-test-connection"
  labels:
{{ include "foo745.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo745.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
