apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar1328.fullname" . }}-test-connection"
  labels:
{{ include "bar1328.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar1328.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
