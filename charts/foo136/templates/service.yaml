apiVersion: v1
kind: Service
metadata:
  name: {{ include "foo136.fullname" . }}
  labels:
{{ include "foo136.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "foo136.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
