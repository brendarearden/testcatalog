{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo136.serviceAccountName" . }}
  labels:
{{ include "foo136.labels" . | indent 4 }}
{{- end -}}
