apiVersion: v1
kind: Service
metadata:
  name: {{ include "moo3744.fullname" . }}
  labels:
{{ include "moo3744.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "moo3744.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
