{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo729.serviceAccountName" . }}
  labels:
{{ include "foo729.labels" . | indent 4 }}
{{- end -}}
