{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo822.serviceAccountName" . }}
  labels:
{{ include "foo822.labels" . | indent 4 }}
{{- end -}}
