{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar556.serviceAccountName" . }}
  labels:
{{ include "bar556.labels" . | indent 4 }}
{{- end -}}
