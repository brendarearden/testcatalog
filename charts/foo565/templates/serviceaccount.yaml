{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo565.serviceAccountName" . }}
  labels:
{{ include "foo565.labels" . | indent 4 }}
{{- end -}}
