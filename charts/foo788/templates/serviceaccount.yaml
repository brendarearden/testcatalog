{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo788.serviceAccountName" . }}
  labels:
{{ include "foo788.labels" . | indent 4 }}
{{- end -}}
