apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo368.fullname" . }}-test-connection"
  labels:
{{ include "foo368.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo368.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
