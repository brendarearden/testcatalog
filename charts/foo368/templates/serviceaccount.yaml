{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo368.serviceAccountName" . }}
  labels:
{{ include "foo368.labels" . | indent 4 }}
{{- end -}}
