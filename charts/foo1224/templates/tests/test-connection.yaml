apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo1224.fullname" . }}-test-connection"
  labels:
{{ include "foo1224.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo1224.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
