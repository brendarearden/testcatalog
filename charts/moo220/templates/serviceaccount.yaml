{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo220.serviceAccountName" . }}
  labels:
{{ include "moo220.labels" . | indent 4 }}
{{- end -}}
