{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar452.serviceAccountName" . }}
  labels:
{{ include "bar452.labels" . | indent 4 }}
{{- end -}}
