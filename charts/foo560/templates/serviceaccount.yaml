{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo560.serviceAccountName" . }}
  labels:
{{ include "foo560.labels" . | indent 4 }}
{{- end -}}
