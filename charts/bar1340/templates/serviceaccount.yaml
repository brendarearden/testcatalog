{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1340.serviceAccountName" . }}
  labels:
{{ include "bar1340.labels" . | indent 4 }}
{{- end -}}
