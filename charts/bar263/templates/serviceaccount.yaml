{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar263.serviceAccountName" . }}
  labels:
{{ include "bar263.labels" . | indent 4 }}
{{- end -}}
