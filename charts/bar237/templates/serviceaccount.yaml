{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar237.serviceAccountName" . }}
  labels:
{{ include "bar237.labels" . | indent 4 }}
{{- end -}}
