{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo339.serviceAccountName" . }}
  labels:
{{ include "foo339.labels" . | indent 4 }}
{{- end -}}
