{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo928.serviceAccountName" . }}
  labels:
{{ include "foo928.labels" . | indent 4 }}
{{- end -}}
