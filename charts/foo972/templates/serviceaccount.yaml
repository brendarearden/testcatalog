{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo972.serviceAccountName" . }}
  labels:
{{ include "foo972.labels" . | indent 4 }}
{{- end -}}
