{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1309.serviceAccountName" . }}
  labels:
{{ include "bar1309.labels" . | indent 4 }}
{{- end -}}
