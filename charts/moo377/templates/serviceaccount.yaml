{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo377.serviceAccountName" . }}
  labels:
{{ include "moo377.labels" . | indent 4 }}
{{- end -}}
