apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo2251.fullname" . }}-test-connection"
  labels:
{{ include "moo2251.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo2251.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
