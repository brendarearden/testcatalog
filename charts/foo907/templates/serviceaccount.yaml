{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo907.serviceAccountName" . }}
  labels:
{{ include "foo907.labels" . | indent 4 }}
{{- end -}}
