{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar95.serviceAccountName" . }}
  labels:
{{ include "bar95.labels" . | indent 4 }}
{{- end -}}
