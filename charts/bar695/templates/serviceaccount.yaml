{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar695.serviceAccountName" . }}
  labels:
{{ include "bar695.labels" . | indent 4 }}
{{- end -}}
