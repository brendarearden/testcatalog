{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1005.serviceAccountName" . }}
  labels:
{{ include "bar1005.labels" . | indent 4 }}
{{- end -}}
