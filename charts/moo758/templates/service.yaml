apiVersion: v1
kind: Service
metadata:
  name: {{ include "moo758.fullname" . }}
  labels:
{{ include "moo758.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "moo758.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
