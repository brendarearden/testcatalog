{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar686.serviceAccountName" . }}
  labels:
{{ include "bar686.labels" . | indent 4 }}
{{- end -}}
