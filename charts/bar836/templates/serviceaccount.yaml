{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar836.serviceAccountName" . }}
  labels:
{{ include "bar836.labels" . | indent 4 }}
{{- end -}}
