{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1228.serviceAccountName" . }}
  labels:
{{ include "foo1228.labels" . | indent 4 }}
{{- end -}}
