{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar110.serviceAccountName" . }}
  labels:
{{ include "bar110.labels" . | indent 4 }}
{{- end -}}
