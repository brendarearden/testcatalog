{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo2958.serviceAccountName" . }}
  labels:
{{ include "foo2958.labels" . | indent 4 }}
{{- end -}}
