{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar818.serviceAccountName" . }}
  labels:
{{ include "bar818.labels" . | indent 4 }}
{{- end -}}
