apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo508.fullname" . }}-test-connection"
  labels:
{{ include "moo508.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo508.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
