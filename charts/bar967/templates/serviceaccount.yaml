{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar967.serviceAccountName" . }}
  labels:
{{ include "bar967.labels" . | indent 4 }}
{{- end -}}
