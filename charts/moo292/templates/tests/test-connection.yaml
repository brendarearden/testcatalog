apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo292.fullname" . }}-test-connection"
  labels:
{{ include "moo292.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo292.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
