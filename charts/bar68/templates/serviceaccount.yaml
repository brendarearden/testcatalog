{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar68.serviceAccountName" . }}
  labels:
{{ include "bar68.labels" . | indent 4 }}
{{- end -}}
