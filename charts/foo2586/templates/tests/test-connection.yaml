apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo2586.fullname" . }}-test-connection"
  labels:
{{ include "foo2586.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo2586.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
