{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1298.serviceAccountName" . }}
  labels:
{{ include "bar1298.labels" . | indent 4 }}
{{- end -}}
