apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo3262.fullname" . }}-test-connection"
  labels:
{{ include "moo3262.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo3262.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
