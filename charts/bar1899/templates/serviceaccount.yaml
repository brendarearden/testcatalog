{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1899.serviceAccountName" . }}
  labels:
{{ include "bar1899.labels" . | indent 4 }}
{{- end -}}
