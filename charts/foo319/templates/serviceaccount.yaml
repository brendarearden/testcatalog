{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo319.serviceAccountName" . }}
  labels:
{{ include "foo319.labels" . | indent 4 }}
{{- end -}}
