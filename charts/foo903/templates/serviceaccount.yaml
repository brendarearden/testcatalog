{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo903.serviceAccountName" . }}
  labels:
{{ include "foo903.labels" . | indent 4 }}
{{- end -}}
