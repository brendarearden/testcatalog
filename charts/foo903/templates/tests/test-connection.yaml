apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo903.fullname" . }}-test-connection"
  labels:
{{ include "foo903.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo903.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
