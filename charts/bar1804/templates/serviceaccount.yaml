{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1804.serviceAccountName" . }}
  labels:
{{ include "bar1804.labels" . | indent 4 }}
{{- end -}}
