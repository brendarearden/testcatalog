{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo391.serviceAccountName" . }}
  labels:
{{ include "foo391.labels" . | indent 4 }}
{{- end -}}
