apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo391.fullname" . }}-test-connection"
  labels:
{{ include "foo391.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo391.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
