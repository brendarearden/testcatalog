{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1006.serviceAccountName" . }}
  labels:
{{ include "bar1006.labels" . | indent 4 }}
{{- end -}}
