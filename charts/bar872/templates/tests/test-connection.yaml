apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar872.fullname" . }}-test-connection"
  labels:
{{ include "bar872.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar872.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
