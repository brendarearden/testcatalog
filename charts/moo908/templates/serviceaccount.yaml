{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo908.serviceAccountName" . }}
  labels:
{{ include "moo908.labels" . | indent 4 }}
{{- end -}}
