{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar612.serviceAccountName" . }}
  labels:
{{ include "bar612.labels" . | indent 4 }}
{{- end -}}
