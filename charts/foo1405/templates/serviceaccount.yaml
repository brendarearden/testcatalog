{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1405.serviceAccountName" . }}
  labels:
{{ include "foo1405.labels" . | indent 4 }}
{{- end -}}
