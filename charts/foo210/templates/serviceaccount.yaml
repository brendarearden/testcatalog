{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo210.serviceAccountName" . }}
  labels:
{{ include "foo210.labels" . | indent 4 }}
{{- end -}}
