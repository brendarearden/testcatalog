apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo210.fullname" . }}-test-connection"
  labels:
{{ include "foo210.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo210.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
