{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo502.serviceAccountName" . }}
  labels:
{{ include "foo502.labels" . | indent 4 }}
{{- end -}}
