{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar349.serviceAccountName" . }}
  labels:
{{ include "bar349.labels" . | indent 4 }}
{{- end -}}
