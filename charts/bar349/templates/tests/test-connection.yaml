apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar349.fullname" . }}-test-connection"
  labels:
{{ include "bar349.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar349.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
