{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo23.serviceAccountName" . }}
  labels:
{{ include "foo23.labels" . | indent 4 }}
{{- end -}}
