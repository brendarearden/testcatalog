{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo512.serviceAccountName" . }}
  labels:
{{ include "foo512.labels" . | indent 4 }}
{{- end -}}
