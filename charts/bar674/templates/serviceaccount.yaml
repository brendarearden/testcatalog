{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar674.serviceAccountName" . }}
  labels:
{{ include "bar674.labels" . | indent 4 }}
{{- end -}}
