{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar219.serviceAccountName" . }}
  labels:
{{ include "bar219.labels" . | indent 4 }}
{{- end -}}
