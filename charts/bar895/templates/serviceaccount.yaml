{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar895.serviceAccountName" . }}
  labels:
{{ include "bar895.labels" . | indent 4 }}
{{- end -}}
