{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar22.serviceAccountName" . }}
  labels:
{{ include "bar22.labels" . | indent 4 }}
{{- end -}}
