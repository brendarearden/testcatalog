{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar909.serviceAccountName" . }}
  labels:
{{ include "bar909.labels" . | indent 4 }}
{{- end -}}
