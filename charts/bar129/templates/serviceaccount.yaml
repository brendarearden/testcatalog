{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar129.serviceAccountName" . }}
  labels:
{{ include "bar129.labels" . | indent 4 }}
{{- end -}}
