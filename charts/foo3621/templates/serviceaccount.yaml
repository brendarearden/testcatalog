{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo3621.serviceAccountName" . }}
  labels:
{{ include "foo3621.labels" . | indent 4 }}
{{- end -}}
