{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo77.serviceAccountName" . }}
  labels:
{{ include "foo77.labels" . | indent 4 }}
{{- end -}}
