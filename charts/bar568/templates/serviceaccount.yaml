{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar568.serviceAccountName" . }}
  labels:
{{ include "bar568.labels" . | indent 4 }}
{{- end -}}
