apiVersion: v1
kind: Service
metadata:
  name: {{ include "moo677.fullname" . }}
  labels:
{{ include "moo677.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "moo677.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
