{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1944.serviceAccountName" . }}
  labels:
{{ include "bar1944.labels" . | indent 4 }}
{{- end -}}
