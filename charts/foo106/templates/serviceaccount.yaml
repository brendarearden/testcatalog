{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo106.serviceAccountName" . }}
  labels:
{{ include "foo106.labels" . | indent 4 }}
{{- end -}}
