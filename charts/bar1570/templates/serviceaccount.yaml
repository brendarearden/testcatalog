{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1570.serviceAccountName" . }}
  labels:
{{ include "bar1570.labels" . | indent 4 }}
{{- end -}}
