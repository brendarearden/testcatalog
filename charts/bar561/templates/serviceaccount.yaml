{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar561.serviceAccountName" . }}
  labels:
{{ include "bar561.labels" . | indent 4 }}
{{- end -}}
