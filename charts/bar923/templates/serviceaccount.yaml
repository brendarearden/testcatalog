{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar923.serviceAccountName" . }}
  labels:
{{ include "bar923.labels" . | indent 4 }}
{{- end -}}
