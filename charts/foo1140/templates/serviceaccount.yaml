{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1140.serviceAccountName" . }}
  labels:
{{ include "foo1140.labels" . | indent 4 }}
{{- end -}}
