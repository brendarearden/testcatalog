{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo828.serviceAccountName" . }}
  labels:
{{ include "moo828.labels" . | indent 4 }}
{{- end -}}
