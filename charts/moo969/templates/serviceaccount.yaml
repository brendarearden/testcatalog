{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo969.serviceAccountName" . }}
  labels:
{{ include "moo969.labels" . | indent 4 }}
{{- end -}}
