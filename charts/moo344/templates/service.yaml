apiVersion: v1
kind: Service
metadata:
  name: {{ include "moo344.fullname" . }}
  labels:
{{ include "moo344.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "moo344.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
