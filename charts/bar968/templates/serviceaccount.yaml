{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar968.serviceAccountName" . }}
  labels:
{{ include "bar968.labels" . | indent 4 }}
{{- end -}}
