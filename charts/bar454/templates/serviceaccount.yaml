{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar454.serviceAccountName" . }}
  labels:
{{ include "bar454.labels" . | indent 4 }}
{{- end -}}
