{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar335.serviceAccountName" . }}
  labels:
{{ include "bar335.labels" . | indent 4 }}
{{- end -}}
