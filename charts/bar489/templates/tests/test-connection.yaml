apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar489.fullname" . }}-test-connection"
  labels:
{{ include "bar489.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar489.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
