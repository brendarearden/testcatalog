apiVersion: v1
kind: Service
metadata:
  name: {{ include "bar961.fullname" . }}
  labels:
{{ include "bar961.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "bar961.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
