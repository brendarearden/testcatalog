{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar961.serviceAccountName" . }}
  labels:
{{ include "bar961.labels" . | indent 4 }}
{{- end -}}
