apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo2686.fullname" . }}-test-connection"
  labels:
{{ include "moo2686.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo2686.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
