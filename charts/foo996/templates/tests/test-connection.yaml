apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo996.fullname" . }}-test-connection"
  labels:
{{ include "foo996.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo996.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
