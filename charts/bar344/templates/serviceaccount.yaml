{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar344.serviceAccountName" . }}
  labels:
{{ include "bar344.labels" . | indent 4 }}
{{- end -}}
