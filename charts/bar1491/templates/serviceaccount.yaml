{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1491.serviceAccountName" . }}
  labels:
{{ include "bar1491.labels" . | indent 4 }}
{{- end -}}
