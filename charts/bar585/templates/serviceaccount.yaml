{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar585.serviceAccountName" . }}
  labels:
{{ include "bar585.labels" . | indent 4 }}
{{- end -}}
