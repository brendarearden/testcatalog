{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo312.serviceAccountName" . }}
  labels:
{{ include "foo312.labels" . | indent 4 }}
{{- end -}}
