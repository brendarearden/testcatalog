{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo444.serviceAccountName" . }}
  labels:
{{ include "moo444.labels" . | indent 4 }}
{{- end -}}
