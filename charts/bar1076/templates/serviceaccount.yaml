{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1076.serviceAccountName" . }}
  labels:
{{ include "bar1076.labels" . | indent 4 }}
{{- end -}}
