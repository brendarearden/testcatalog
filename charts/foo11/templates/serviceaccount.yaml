{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo11.serviceAccountName" . }}
  labels:
{{ include "foo11.labels" . | indent 4 }}
{{- end -}}
