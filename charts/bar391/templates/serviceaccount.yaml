{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar391.serviceAccountName" . }}
  labels:
{{ include "bar391.labels" . | indent 4 }}
{{- end -}}
