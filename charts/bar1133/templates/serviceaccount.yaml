{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1133.serviceAccountName" . }}
  labels:
{{ include "bar1133.labels" . | indent 4 }}
{{- end -}}
