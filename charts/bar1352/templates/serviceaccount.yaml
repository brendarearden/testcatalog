{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1352.serviceAccountName" . }}
  labels:
{{ include "bar1352.labels" . | indent 4 }}
{{- end -}}
