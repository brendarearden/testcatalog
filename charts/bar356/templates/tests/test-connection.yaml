apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar356.fullname" . }}-test-connection"
  labels:
{{ include "bar356.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar356.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
