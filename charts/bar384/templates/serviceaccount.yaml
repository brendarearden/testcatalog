{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar384.serviceAccountName" . }}
  labels:
{{ include "bar384.labels" . | indent 4 }}
{{- end -}}
