{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1172.serviceAccountName" . }}
  labels:
{{ include "foo1172.labels" . | indent 4 }}
{{- end -}}
