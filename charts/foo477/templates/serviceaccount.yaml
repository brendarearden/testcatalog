{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo477.serviceAccountName" . }}
  labels:
{{ include "foo477.labels" . | indent 4 }}
{{- end -}}
