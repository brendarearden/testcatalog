apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo3959.fullname" . }}-test-connection"
  labels:
{{ include "foo3959.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo3959.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
