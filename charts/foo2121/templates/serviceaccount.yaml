{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo2121.serviceAccountName" . }}
  labels:
{{ include "foo2121.labels" . | indent 4 }}
{{- end -}}
