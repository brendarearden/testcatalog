{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo417.serviceAccountName" . }}
  labels:
{{ include "foo417.labels" . | indent 4 }}
{{- end -}}
