apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo1584.fullname" . }}-test-connection"
  labels:
{{ include "moo1584.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo1584.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
