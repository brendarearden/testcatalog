{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar53.serviceAccountName" . }}
  labels:
{{ include "bar53.labels" . | indent 4 }}
{{- end -}}
