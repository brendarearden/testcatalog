apiVersion: v1
kind: Service
metadata:
  name: {{ include "moo647.fullname" . }}
  labels:
{{ include "moo647.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "moo647.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
