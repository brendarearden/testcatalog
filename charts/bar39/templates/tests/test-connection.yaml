apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar39.fullname" . }}-test-connection"
  labels:
{{ include "bar39.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar39.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
