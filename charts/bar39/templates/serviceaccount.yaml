{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar39.serviceAccountName" . }}
  labels:
{{ include "bar39.labels" . | indent 4 }}
{{- end -}}
