{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo3119.serviceAccountName" . }}
  labels:
{{ include "foo3119.labels" . | indent 4 }}
{{- end -}}
