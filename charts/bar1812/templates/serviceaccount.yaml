{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1812.serviceAccountName" . }}
  labels:
{{ include "bar1812.labels" . | indent 4 }}
{{- end -}}
