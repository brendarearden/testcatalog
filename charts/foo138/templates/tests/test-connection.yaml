apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo138.fullname" . }}-test-connection"
  labels:
{{ include "foo138.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo138.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
