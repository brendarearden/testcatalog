{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo227.serviceAccountName" . }}
  labels:
{{ include "foo227.labels" . | indent 4 }}
{{- end -}}
