{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1790.serviceAccountName" . }}
  labels:
{{ include "bar1790.labels" . | indent 4 }}
{{- end -}}
