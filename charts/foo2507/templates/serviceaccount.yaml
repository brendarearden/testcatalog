{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo2507.serviceAccountName" . }}
  labels:
{{ include "foo2507.labels" . | indent 4 }}
{{- end -}}
