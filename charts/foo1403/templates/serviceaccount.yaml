{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1403.serviceAccountName" . }}
  labels:
{{ include "foo1403.labels" . | indent 4 }}
{{- end -}}
