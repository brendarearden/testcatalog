apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo50.fullname" . }}-test-connection"
  labels:
{{ include "moo50.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo50.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
