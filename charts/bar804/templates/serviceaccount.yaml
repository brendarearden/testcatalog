{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar804.serviceAccountName" . }}
  labels:
{{ include "bar804.labels" . | indent 4 }}
{{- end -}}
