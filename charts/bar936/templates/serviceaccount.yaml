{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar936.serviceAccountName" . }}
  labels:
{{ include "bar936.labels" . | indent 4 }}
{{- end -}}
