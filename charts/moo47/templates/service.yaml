apiVersion: v1
kind: Service
metadata:
  name: {{ include "moo47.fullname" . }}
  labels:
{{ include "moo47.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "moo47.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
