apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar29.fullname" . }}-test-connection"
  labels:
{{ include "bar29.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar29.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
