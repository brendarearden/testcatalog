{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo331.serviceAccountName" . }}
  labels:
{{ include "foo331.labels" . | indent 4 }}
{{- end -}}
