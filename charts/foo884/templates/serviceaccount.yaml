{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo884.serviceAccountName" . }}
  labels:
{{ include "foo884.labels" . | indent 4 }}
{{- end -}}
