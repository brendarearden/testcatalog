{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo647.serviceAccountName" . }}
  labels:
{{ include "foo647.labels" . | indent 4 }}
{{- end -}}
