apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo840.fullname" . }}-test-connection"
  labels:
{{ include "foo840.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo840.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
