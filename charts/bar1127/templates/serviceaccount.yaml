{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1127.serviceAccountName" . }}
  labels:
{{ include "bar1127.labels" . | indent 4 }}
{{- end -}}
