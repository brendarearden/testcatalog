{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar462.serviceAccountName" . }}
  labels:
{{ include "bar462.labels" . | indent 4 }}
{{- end -}}
