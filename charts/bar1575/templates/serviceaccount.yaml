{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1575.serviceAccountName" . }}
  labels:
{{ include "bar1575.labels" . | indent 4 }}
{{- end -}}
