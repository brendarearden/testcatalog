{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo2745.serviceAccountName" . }}
  labels:
{{ include "moo2745.labels" . | indent 4 }}
{{- end -}}
