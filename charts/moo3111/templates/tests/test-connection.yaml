apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo3111.fullname" . }}-test-connection"
  labels:
{{ include "moo3111.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo3111.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
