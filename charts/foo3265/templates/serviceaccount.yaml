{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo3265.serviceAccountName" . }}
  labels:
{{ include "foo3265.labels" . | indent 4 }}
{{- end -}}
