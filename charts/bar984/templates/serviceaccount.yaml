{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar984.serviceAccountName" . }}
  labels:
{{ include "bar984.labels" . | indent 4 }}
{{- end -}}
