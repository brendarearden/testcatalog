{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1040.serviceAccountName" . }}
  labels:
{{ include "foo1040.labels" . | indent 4 }}
{{- end -}}
