{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo871.serviceAccountName" . }}
  labels:
{{ include "foo871.labels" . | indent 4 }}
{{- end -}}
