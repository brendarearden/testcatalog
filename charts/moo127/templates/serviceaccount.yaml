{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo127.serviceAccountName" . }}
  labels:
{{ include "moo127.labels" . | indent 4 }}
{{- end -}}
