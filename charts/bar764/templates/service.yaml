apiVersion: v1
kind: Service
metadata:
  name: {{ include "bar764.fullname" . }}
  labels:
{{ include "bar764.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "bar764.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
