{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1642.serviceAccountName" . }}
  labels:
{{ include "foo1642.labels" . | indent 4 }}
{{- end -}}
