{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar683.serviceAccountName" . }}
  labels:
{{ include "bar683.labels" . | indent 4 }}
{{- end -}}
