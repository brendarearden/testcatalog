{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo344.serviceAccountName" . }}
  labels:
{{ include "foo344.labels" . | indent 4 }}
{{- end -}}
