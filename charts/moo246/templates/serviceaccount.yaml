{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo246.serviceAccountName" . }}
  labels:
{{ include "moo246.labels" . | indent 4 }}
{{- end -}}
