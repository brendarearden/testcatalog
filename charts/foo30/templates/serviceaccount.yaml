{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo30.serviceAccountName" . }}
  labels:
{{ include "foo30.labels" . | indent 4 }}
{{- end -}}
