apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo30.fullname" . }}-test-connection"
  labels:
{{ include "foo30.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo30.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
