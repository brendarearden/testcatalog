apiVersion: v1
kind: Service
metadata:
  name: {{ include "bar863.fullname" . }}
  labels:
{{ include "bar863.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "bar863.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
