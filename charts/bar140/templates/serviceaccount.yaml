{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar140.serviceAccountName" . }}
  labels:
{{ include "bar140.labels" . | indent 4 }}
{{- end -}}
