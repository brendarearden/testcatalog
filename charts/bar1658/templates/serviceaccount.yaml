{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1658.serviceAccountName" . }}
  labels:
{{ include "bar1658.labels" . | indent 4 }}
{{- end -}}
