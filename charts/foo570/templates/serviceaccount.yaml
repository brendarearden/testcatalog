{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo570.serviceAccountName" . }}
  labels:
{{ include "foo570.labels" . | indent 4 }}
{{- end -}}
