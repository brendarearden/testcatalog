{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo596.serviceAccountName" . }}
  labels:
{{ include "foo596.labels" . | indent 4 }}
{{- end -}}
