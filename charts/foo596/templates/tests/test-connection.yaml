apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo596.fullname" . }}-test-connection"
  labels:
{{ include "foo596.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo596.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
