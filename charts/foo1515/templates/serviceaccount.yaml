{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1515.serviceAccountName" . }}
  labels:
{{ include "foo1515.labels" . | indent 4 }}
{{- end -}}
