{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar393.serviceAccountName" . }}
  labels:
{{ include "bar393.labels" . | indent 4 }}
{{- end -}}
