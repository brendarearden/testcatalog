{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar915.serviceAccountName" . }}
  labels:
{{ include "bar915.labels" . | indent 4 }}
{{- end -}}
