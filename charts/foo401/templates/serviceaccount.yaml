{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo401.serviceAccountName" . }}
  labels:
{{ include "foo401.labels" . | indent 4 }}
{{- end -}}
