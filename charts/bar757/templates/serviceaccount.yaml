{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar757.serviceAccountName" . }}
  labels:
{{ include "bar757.labels" . | indent 4 }}
{{- end -}}
