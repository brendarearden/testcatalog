{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1023.serviceAccountName" . }}
  labels:
{{ include "bar1023.labels" . | indent 4 }}
{{- end -}}
