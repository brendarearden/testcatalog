{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1701.serviceAccountName" . }}
  labels:
{{ include "bar1701.labels" . | indent 4 }}
{{- end -}}
