apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo843.fullname" . }}-test-connection"
  labels:
{{ include "moo843.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo843.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
