{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar207.serviceAccountName" . }}
  labels:
{{ include "bar207.labels" . | indent 4 }}
{{- end -}}
