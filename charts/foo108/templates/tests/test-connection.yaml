apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo108.fullname" . }}-test-connection"
  labels:
{{ include "foo108.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo108.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
