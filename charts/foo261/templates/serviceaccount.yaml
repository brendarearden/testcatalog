{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo261.serviceAccountName" . }}
  labels:
{{ include "foo261.labels" . | indent 4 }}
{{- end -}}
