{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar571.serviceAccountName" . }}
  labels:
{{ include "bar571.labels" . | indent 4 }}
{{- end -}}
