{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1602.serviceAccountName" . }}
  labels:
{{ include "bar1602.labels" . | indent 4 }}
{{- end -}}
