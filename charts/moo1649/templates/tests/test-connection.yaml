apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo1649.fullname" . }}-test-connection"
  labels:
{{ include "moo1649.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo1649.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
