{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo97.serviceAccountName" . }}
  labels:
{{ include "foo97.labels" . | indent 4 }}
{{- end -}}
