{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo519.serviceAccountName" . }}
  labels:
{{ include "moo519.labels" . | indent 4 }}
{{- end -}}
