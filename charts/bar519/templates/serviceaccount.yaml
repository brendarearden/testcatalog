{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar519.serviceAccountName" . }}
  labels:
{{ include "bar519.labels" . | indent 4 }}
{{- end -}}
