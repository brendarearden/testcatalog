apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo2279.fullname" . }}-test-connection"
  labels:
{{ include "moo2279.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo2279.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
