{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar211.serviceAccountName" . }}
  labels:
{{ include "bar211.labels" . | indent 4 }}
{{- end -}}
