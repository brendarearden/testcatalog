{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar394.serviceAccountName" . }}
  labels:
{{ include "bar394.labels" . | indent 4 }}
{{- end -}}
