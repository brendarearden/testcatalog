{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar331.serviceAccountName" . }}
  labels:
{{ include "bar331.labels" . | indent 4 }}
{{- end -}}
