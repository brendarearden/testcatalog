{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar825.serviceAccountName" . }}
  labels:
{{ include "bar825.labels" . | indent 4 }}
{{- end -}}
