apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar940.fullname" . }}-test-connection"
  labels:
{{ include "bar940.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar940.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
