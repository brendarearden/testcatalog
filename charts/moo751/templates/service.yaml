apiVersion: v1
kind: Service
metadata:
  name: {{ include "moo751.fullname" . }}
  labels:
{{ include "moo751.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "moo751.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
