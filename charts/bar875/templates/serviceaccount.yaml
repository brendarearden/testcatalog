{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar875.serviceAccountName" . }}
  labels:
{{ include "bar875.labels" . | indent 4 }}
{{- end -}}
