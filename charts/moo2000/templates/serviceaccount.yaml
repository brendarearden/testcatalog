{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo2000.serviceAccountName" . }}
  labels:
{{ include "moo2000.labels" . | indent 4 }}
{{- end -}}
