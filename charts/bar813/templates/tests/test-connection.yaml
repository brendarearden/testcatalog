apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar813.fullname" . }}-test-connection"
  labels:
{{ include "bar813.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar813.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
