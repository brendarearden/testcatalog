{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1945.serviceAccountName" . }}
  labels:
{{ include "bar1945.labels" . | indent 4 }}
{{- end -}}
