{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1882.serviceAccountName" . }}
  labels:
{{ include "bar1882.labels" . | indent 4 }}
{{- end -}}
