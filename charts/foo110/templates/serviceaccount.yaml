{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo110.serviceAccountName" . }}
  labels:
{{ include "foo110.labels" . | indent 4 }}
{{- end -}}
