{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo112.serviceAccountName" . }}
  labels:
{{ include "moo112.labels" . | indent 4 }}
{{- end -}}
