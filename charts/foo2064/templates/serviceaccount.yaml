{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo2064.serviceAccountName" . }}
  labels:
{{ include "foo2064.labels" . | indent 4 }}
{{- end -}}
