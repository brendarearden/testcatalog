{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1634.serviceAccountName" . }}
  labels:
{{ include "bar1634.labels" . | indent 4 }}
{{- end -}}
