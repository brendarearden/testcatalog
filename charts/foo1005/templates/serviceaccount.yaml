{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1005.serviceAccountName" . }}
  labels:
{{ include "foo1005.labels" . | indent 4 }}
{{- end -}}
