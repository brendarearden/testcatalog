{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1608.serviceAccountName" . }}
  labels:
{{ include "foo1608.labels" . | indent 4 }}
{{- end -}}
