{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo191.serviceAccountName" . }}
  labels:
{{ include "foo191.labels" . | indent 4 }}
{{- end -}}
