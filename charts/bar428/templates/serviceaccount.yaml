{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar428.serviceAccountName" . }}
  labels:
{{ include "bar428.labels" . | indent 4 }}
{{- end -}}
