{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar853.serviceAccountName" . }}
  labels:
{{ include "bar853.labels" . | indent 4 }}
{{- end -}}
