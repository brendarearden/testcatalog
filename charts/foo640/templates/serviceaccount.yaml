{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo640.serviceAccountName" . }}
  labels:
{{ include "foo640.labels" . | indent 4 }}
{{- end -}}
