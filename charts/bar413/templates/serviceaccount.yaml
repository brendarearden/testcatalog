{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar413.serviceAccountName" . }}
  labels:
{{ include "bar413.labels" . | indent 4 }}
{{- end -}}
