{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo491.serviceAccountName" . }}
  labels:
{{ include "foo491.labels" . | indent 4 }}
{{- end -}}
