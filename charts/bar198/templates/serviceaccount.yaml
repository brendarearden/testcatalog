{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar198.serviceAccountName" . }}
  labels:
{{ include "bar198.labels" . | indent 4 }}
{{- end -}}
