apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo3448.fullname" . }}-test-connection"
  labels:
{{ include "moo3448.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo3448.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
