{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo589.serviceAccountName" . }}
  labels:
{{ include "foo589.labels" . | indent 4 }}
{{- end -}}
