{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1039.serviceAccountName" . }}
  labels:
{{ include "bar1039.labels" . | indent 4 }}
{{- end -}}
