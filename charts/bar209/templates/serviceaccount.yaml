{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar209.serviceAccountName" . }}
  labels:
{{ include "bar209.labels" . | indent 4 }}
{{- end -}}
