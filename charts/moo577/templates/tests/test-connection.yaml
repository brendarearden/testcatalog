apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo577.fullname" . }}-test-connection"
  labels:
{{ include "moo577.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo577.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
