{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1961.serviceAccountName" . }}
  labels:
{{ include "foo1961.labels" . | indent 4 }}
{{- end -}}
