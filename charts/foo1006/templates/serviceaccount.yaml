{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1006.serviceAccountName" . }}
  labels:
{{ include "foo1006.labels" . | indent 4 }}
{{- end -}}
