{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1024.serviceAccountName" . }}
  labels:
{{ include "foo1024.labels" . | indent 4 }}
{{- end -}}
