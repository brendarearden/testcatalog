{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo2806.serviceAccountName" . }}
  labels:
{{ include "moo2806.labels" . | indent 4 }}
{{- end -}}
