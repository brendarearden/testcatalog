{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar293.serviceAccountName" . }}
  labels:
{{ include "bar293.labels" . | indent 4 }}
{{- end -}}
