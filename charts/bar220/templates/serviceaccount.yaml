{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar220.serviceAccountName" . }}
  labels:
{{ include "bar220.labels" . | indent 4 }}
{{- end -}}
