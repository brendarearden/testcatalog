apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar1735.fullname" . }}-test-connection"
  labels:
{{ include "bar1735.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar1735.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
