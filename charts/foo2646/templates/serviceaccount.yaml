{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo2646.serviceAccountName" . }}
  labels:
{{ include "foo2646.labels" . | indent 4 }}
{{- end -}}
