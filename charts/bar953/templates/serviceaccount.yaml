{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar953.serviceAccountName" . }}
  labels:
{{ include "bar953.labels" . | indent 4 }}
{{- end -}}
