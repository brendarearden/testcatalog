{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar666.serviceAccountName" . }}
  labels:
{{ include "bar666.labels" . | indent 4 }}
{{- end -}}
