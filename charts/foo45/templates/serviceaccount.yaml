{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo45.serviceAccountName" . }}
  labels:
{{ include "foo45.labels" . | indent 4 }}
{{- end -}}
