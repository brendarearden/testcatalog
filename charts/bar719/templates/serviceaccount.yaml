{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar719.serviceAccountName" . }}
  labels:
{{ include "bar719.labels" . | indent 4 }}
{{- end -}}
