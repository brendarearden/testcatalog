{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo2227.serviceAccountName" . }}
  labels:
{{ include "moo2227.labels" . | indent 4 }}
{{- end -}}
