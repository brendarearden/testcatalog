{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo98.serviceAccountName" . }}
  labels:
{{ include "foo98.labels" . | indent 4 }}
{{- end -}}
