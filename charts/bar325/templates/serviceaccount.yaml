{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar325.serviceAccountName" . }}
  labels:
{{ include "bar325.labels" . | indent 4 }}
{{- end -}}
