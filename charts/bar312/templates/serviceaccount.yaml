{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar312.serviceAccountName" . }}
  labels:
{{ include "bar312.labels" . | indent 4 }}
{{- end -}}
