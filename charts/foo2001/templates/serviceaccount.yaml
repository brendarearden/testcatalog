{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo2001.serviceAccountName" . }}
  labels:
{{ include "foo2001.labels" . | indent 4 }}
{{- end -}}
