{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar788.serviceAccountName" . }}
  labels:
{{ include "bar788.labels" . | indent 4 }}
{{- end -}}
