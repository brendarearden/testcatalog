apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar788.fullname" . }}-test-connection"
  labels:
{{ include "bar788.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar788.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
