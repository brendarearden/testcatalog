{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1956.serviceAccountName" . }}
  labels:
{{ include "foo1956.labels" . | indent 4 }}
{{- end -}}
