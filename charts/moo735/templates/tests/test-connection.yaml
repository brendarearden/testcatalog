apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo735.fullname" . }}-test-connection"
  labels:
{{ include "moo735.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo735.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
