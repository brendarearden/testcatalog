{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo79.serviceAccountName" . }}
  labels:
{{ include "moo79.labels" . | indent 4 }}
{{- end -}}
