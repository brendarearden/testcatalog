{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo2300.serviceAccountName" . }}
  labels:
{{ include "moo2300.labels" . | indent 4 }}
{{- end -}}
