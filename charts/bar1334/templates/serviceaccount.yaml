{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1334.serviceAccountName" . }}
  labels:
{{ include "bar1334.labels" . | indent 4 }}
{{- end -}}
