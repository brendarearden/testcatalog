{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar704.serviceAccountName" . }}
  labels:
{{ include "bar704.labels" . | indent 4 }}
{{- end -}}
