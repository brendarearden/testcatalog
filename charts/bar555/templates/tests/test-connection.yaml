apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar555.fullname" . }}-test-connection"
  labels:
{{ include "bar555.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar555.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
