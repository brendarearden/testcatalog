{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo390.serviceAccountName" . }}
  labels:
{{ include "foo390.labels" . | indent 4 }}
{{- end -}}
