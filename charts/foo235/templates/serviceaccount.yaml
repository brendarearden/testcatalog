{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo235.serviceAccountName" . }}
  labels:
{{ include "foo235.labels" . | indent 4 }}
{{- end -}}
