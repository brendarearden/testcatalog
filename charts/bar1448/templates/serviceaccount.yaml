{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1448.serviceAccountName" . }}
  labels:
{{ include "bar1448.labels" . | indent 4 }}
{{- end -}}
