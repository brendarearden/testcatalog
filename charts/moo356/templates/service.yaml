apiVersion: v1
kind: Service
metadata:
  name: {{ include "moo356.fullname" . }}
  labels:
{{ include "moo356.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "moo356.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
