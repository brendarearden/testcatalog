{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar652.serviceAccountName" . }}
  labels:
{{ include "bar652.labels" . | indent 4 }}
{{- end -}}
