{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo9.serviceAccountName" . }}
  labels:
{{ include "foo9.labels" . | indent 4 }}
{{- end -}}
