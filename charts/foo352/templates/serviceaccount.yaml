{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo352.serviceAccountName" . }}
  labels:
{{ include "foo352.labels" . | indent 4 }}
{{- end -}}
