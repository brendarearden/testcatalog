apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo352.fullname" . }}-test-connection"
  labels:
{{ include "foo352.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo352.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
