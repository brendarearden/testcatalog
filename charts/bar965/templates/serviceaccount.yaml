{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar965.serviceAccountName" . }}
  labels:
{{ include "bar965.labels" . | indent 4 }}
{{- end -}}
