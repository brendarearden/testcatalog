apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo3186.fullname" . }}-test-connection"
  labels:
{{ include "moo3186.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo3186.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
