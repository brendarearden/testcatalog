{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1625.serviceAccountName" . }}
  labels:
{{ include "bar1625.labels" . | indent 4 }}
{{- end -}}
