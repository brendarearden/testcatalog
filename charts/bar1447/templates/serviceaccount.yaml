{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1447.serviceAccountName" . }}
  labels:
{{ include "bar1447.labels" . | indent 4 }}
{{- end -}}
