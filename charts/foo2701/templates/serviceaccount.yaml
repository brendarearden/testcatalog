{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo2701.serviceAccountName" . }}
  labels:
{{ include "foo2701.labels" . | indent 4 }}
{{- end -}}
