{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1625.serviceAccountName" . }}
  labels:
{{ include "foo1625.labels" . | indent 4 }}
{{- end -}}
