{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1221.serviceAccountName" . }}
  labels:
{{ include "bar1221.labels" . | indent 4 }}
{{- end -}}
