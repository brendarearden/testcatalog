{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo3506.serviceAccountName" . }}
  labels:
{{ include "foo3506.labels" . | indent 4 }}
{{- end -}}
