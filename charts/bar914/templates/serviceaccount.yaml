{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar914.serviceAccountName" . }}
  labels:
{{ include "bar914.labels" . | indent 4 }}
{{- end -}}
