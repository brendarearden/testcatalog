apiVersion: v1
kind: Service
metadata:
  name: {{ include "moo433.fullname" . }}
  labels:
{{ include "moo433.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "moo433.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
