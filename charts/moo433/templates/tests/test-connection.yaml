apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo433.fullname" . }}-test-connection"
  labels:
{{ include "moo433.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo433.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
