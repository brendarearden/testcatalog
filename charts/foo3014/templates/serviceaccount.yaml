{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo3014.serviceAccountName" . }}
  labels:
{{ include "foo3014.labels" . | indent 4 }}
{{- end -}}
