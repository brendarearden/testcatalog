{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo993.serviceAccountName" . }}
  labels:
{{ include "moo993.labels" . | indent 4 }}
{{- end -}}
