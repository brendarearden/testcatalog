apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo993.fullname" . }}-test-connection"
  labels:
{{ include "moo993.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo993.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
