{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar187.serviceAccountName" . }}
  labels:
{{ include "bar187.labels" . | indent 4 }}
{{- end -}}
