{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar365.serviceAccountName" . }}
  labels:
{{ include "bar365.labels" . | indent 4 }}
{{- end -}}
