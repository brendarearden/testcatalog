apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo386.fullname" . }}-test-connection"
  labels:
{{ include "moo386.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo386.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
