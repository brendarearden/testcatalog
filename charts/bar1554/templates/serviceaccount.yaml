{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1554.serviceAccountName" . }}
  labels:
{{ include "bar1554.labels" . | indent 4 }}
{{- end -}}
