{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1913.serviceAccountName" . }}
  labels:
{{ include "bar1913.labels" . | indent 4 }}
{{- end -}}
