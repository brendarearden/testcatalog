{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1936.serviceAccountName" . }}
  labels:
{{ include "bar1936.labels" . | indent 4 }}
{{- end -}}
