{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar646.serviceAccountName" . }}
  labels:
{{ include "bar646.labels" . | indent 4 }}
{{- end -}}
