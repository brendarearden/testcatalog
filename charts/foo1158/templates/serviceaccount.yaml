{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1158.serviceAccountName" . }}
  labels:
{{ include "foo1158.labels" . | indent 4 }}
{{- end -}}
