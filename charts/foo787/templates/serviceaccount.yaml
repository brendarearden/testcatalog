{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo787.serviceAccountName" . }}
  labels:
{{ include "foo787.labels" . | indent 4 }}
{{- end -}}
