{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar938.serviceAccountName" . }}
  labels:
{{ include "bar938.labels" . | indent 4 }}
{{- end -}}
