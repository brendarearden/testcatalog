{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo870.serviceAccountName" . }}
  labels:
{{ include "foo870.labels" . | indent 4 }}
{{- end -}}
