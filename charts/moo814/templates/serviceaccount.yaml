{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo814.serviceAccountName" . }}
  labels:
{{ include "moo814.labels" . | indent 4 }}
{{- end -}}
