{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo3112.serviceAccountName" . }}
  labels:
{{ include "foo3112.labels" . | indent 4 }}
{{- end -}}
