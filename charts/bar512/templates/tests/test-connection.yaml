apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar512.fullname" . }}-test-connection"
  labels:
{{ include "bar512.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar512.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
