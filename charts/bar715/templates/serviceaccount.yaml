{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar715.serviceAccountName" . }}
  labels:
{{ include "bar715.labels" . | indent 4 }}
{{- end -}}
