apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar260.fullname" . }}-test-connection"
  labels:
{{ include "bar260.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar260.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
