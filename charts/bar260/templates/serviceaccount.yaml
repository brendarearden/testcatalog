{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar260.serviceAccountName" . }}
  labels:
{{ include "bar260.labels" . | indent 4 }}
{{- end -}}
