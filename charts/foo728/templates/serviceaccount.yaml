{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo728.serviceAccountName" . }}
  labels:
{{ include "foo728.labels" . | indent 4 }}
{{- end -}}
