apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar682.fullname" . }}-test-connection"
  labels:
{{ include "bar682.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar682.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
