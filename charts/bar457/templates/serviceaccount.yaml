{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar457.serviceAccountName" . }}
  labels:
{{ include "bar457.labels" . | indent 4 }}
{{- end -}}
