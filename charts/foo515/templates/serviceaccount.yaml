{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo515.serviceAccountName" . }}
  labels:
{{ include "foo515.labels" . | indent 4 }}
{{- end -}}
