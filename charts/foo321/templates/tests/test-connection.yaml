apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo321.fullname" . }}-test-connection"
  labels:
{{ include "foo321.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo321.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
