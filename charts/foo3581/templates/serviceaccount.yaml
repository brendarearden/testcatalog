{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo3581.serviceAccountName" . }}
  labels:
{{ include "foo3581.labels" . | indent 4 }}
{{- end -}}
