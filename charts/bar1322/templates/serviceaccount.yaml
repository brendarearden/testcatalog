{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1322.serviceAccountName" . }}
  labels:
{{ include "bar1322.labels" . | indent 4 }}
{{- end -}}
