{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo4000.serviceAccountName" . }}
  labels:
{{ include "foo4000.labels" . | indent 4 }}
{{- end -}}
