apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo4000.fullname" . }}-test-connection"
  labels:
{{ include "foo4000.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo4000.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
