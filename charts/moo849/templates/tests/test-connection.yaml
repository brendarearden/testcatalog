apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo849.fullname" . }}-test-connection"
  labels:
{{ include "moo849.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo849.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
