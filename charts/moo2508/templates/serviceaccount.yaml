{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo2508.serviceAccountName" . }}
  labels:
{{ include "moo2508.labels" . | indent 4 }}
{{- end -}}
