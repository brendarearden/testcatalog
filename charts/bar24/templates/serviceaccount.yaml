{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar24.serviceAccountName" . }}
  labels:
{{ include "bar24.labels" . | indent 4 }}
{{- end -}}
