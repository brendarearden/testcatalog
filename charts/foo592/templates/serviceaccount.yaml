{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo592.serviceAccountName" . }}
  labels:
{{ include "foo592.labels" . | indent 4 }}
{{- end -}}
