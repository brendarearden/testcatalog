{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar406.serviceAccountName" . }}
  labels:
{{ include "bar406.labels" . | indent 4 }}
{{- end -}}
