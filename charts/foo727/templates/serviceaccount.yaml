{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo727.serviceAccountName" . }}
  labels:
{{ include "foo727.labels" . | indent 4 }}
{{- end -}}
