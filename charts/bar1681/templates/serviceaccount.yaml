{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1681.serviceAccountName" . }}
  labels:
{{ include "bar1681.labels" . | indent 4 }}
{{- end -}}
