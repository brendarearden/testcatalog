{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1040.serviceAccountName" . }}
  labels:
{{ include "bar1040.labels" . | indent 4 }}
{{- end -}}
