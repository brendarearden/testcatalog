apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo36.fullname" . }}-test-connection"
  labels:
{{ include "foo36.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo36.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
