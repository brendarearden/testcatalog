{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo174.serviceAccountName" . }}
  labels:
{{ include "moo174.labels" . | indent 4 }}
{{- end -}}
