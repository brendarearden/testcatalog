{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo2514.serviceAccountName" . }}
  labels:
{{ include "foo2514.labels" . | indent 4 }}
{{- end -}}
