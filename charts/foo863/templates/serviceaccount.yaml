{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo863.serviceAccountName" . }}
  labels:
{{ include "foo863.labels" . | indent 4 }}
{{- end -}}
