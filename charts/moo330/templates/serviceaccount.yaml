{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo330.serviceAccountName" . }}
  labels:
{{ include "moo330.labels" . | indent 4 }}
{{- end -}}
