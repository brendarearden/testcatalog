{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo925.serviceAccountName" . }}
  labels:
{{ include "foo925.labels" . | indent 4 }}
{{- end -}}
