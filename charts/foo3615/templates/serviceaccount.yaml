{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo3615.serviceAccountName" . }}
  labels:
{{ include "foo3615.labels" . | indent 4 }}
{{- end -}}
