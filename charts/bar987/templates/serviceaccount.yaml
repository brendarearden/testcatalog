{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar987.serviceAccountName" . }}
  labels:
{{ include "bar987.labels" . | indent 4 }}
{{- end -}}
