apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar493.fullname" . }}-test-connection"
  labels:
{{ include "bar493.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar493.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
