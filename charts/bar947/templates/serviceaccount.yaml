{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar947.serviceAccountName" . }}
  labels:
{{ include "bar947.labels" . | indent 4 }}
{{- end -}}
