{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo513.serviceAccountName" . }}
  labels:
{{ include "foo513.labels" . | indent 4 }}
{{- end -}}
