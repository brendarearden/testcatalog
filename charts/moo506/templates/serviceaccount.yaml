{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo506.serviceAccountName" . }}
  labels:
{{ include "moo506.labels" . | indent 4 }}
{{- end -}}
