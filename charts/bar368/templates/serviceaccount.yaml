{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar368.serviceAccountName" . }}
  labels:
{{ include "bar368.labels" . | indent 4 }}
{{- end -}}
