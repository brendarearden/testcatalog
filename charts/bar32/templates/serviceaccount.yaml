{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar32.serviceAccountName" . }}
  labels:
{{ include "bar32.labels" . | indent 4 }}
{{- end -}}
