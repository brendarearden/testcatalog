{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar436.serviceAccountName" . }}
  labels:
{{ include "bar436.labels" . | indent 4 }}
{{- end -}}
