apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo183.fullname" . }}-test-connection"
  labels:
{{ include "moo183.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo183.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
