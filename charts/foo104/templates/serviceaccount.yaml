{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo104.serviceAccountName" . }}
  labels:
{{ include "foo104.labels" . | indent 4 }}
{{- end -}}
