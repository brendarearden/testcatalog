{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1331.serviceAccountName" . }}
  labels:
{{ include "bar1331.labels" . | indent 4 }}
{{- end -}}
