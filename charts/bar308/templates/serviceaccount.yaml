{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar308.serviceAccountName" . }}
  labels:
{{ include "bar308.labels" . | indent 4 }}
{{- end -}}
