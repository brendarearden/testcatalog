{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo327.serviceAccountName" . }}
  labels:
{{ include "foo327.labels" . | indent 4 }}
{{- end -}}
