{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar257.serviceAccountName" . }}
  labels:
{{ include "bar257.labels" . | indent 4 }}
{{- end -}}
