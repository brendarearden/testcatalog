apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar257.fullname" . }}-test-connection"
  labels:
{{ include "bar257.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar257.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
