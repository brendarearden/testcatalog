{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo917.serviceAccountName" . }}
  labels:
{{ include "foo917.labels" . | indent 4 }}
{{- end -}}
