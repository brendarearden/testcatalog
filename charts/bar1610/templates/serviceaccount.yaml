{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1610.serviceAccountName" . }}
  labels:
{{ include "bar1610.labels" . | indent 4 }}
{{- end -}}
