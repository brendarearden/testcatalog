apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar558.fullname" . }}-test-connection"
  labels:
{{ include "bar558.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar558.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
