{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar732.serviceAccountName" . }}
  labels:
{{ include "bar732.labels" . | indent 4 }}
{{- end -}}
