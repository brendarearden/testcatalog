{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1461.serviceAccountName" . }}
  labels:
{{ include "bar1461.labels" . | indent 4 }}
{{- end -}}
