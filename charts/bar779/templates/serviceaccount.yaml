{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar779.serviceAccountName" . }}
  labels:
{{ include "bar779.labels" . | indent 4 }}
{{- end -}}
