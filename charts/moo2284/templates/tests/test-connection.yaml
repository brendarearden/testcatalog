apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo2284.fullname" . }}-test-connection"
  labels:
{{ include "moo2284.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo2284.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
