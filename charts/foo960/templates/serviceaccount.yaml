{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo960.serviceAccountName" . }}
  labels:
{{ include "foo960.labels" . | indent 4 }}
{{- end -}}
