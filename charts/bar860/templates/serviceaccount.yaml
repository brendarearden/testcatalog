{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar860.serviceAccountName" . }}
  labels:
{{ include "bar860.labels" . | indent 4 }}
{{- end -}}
