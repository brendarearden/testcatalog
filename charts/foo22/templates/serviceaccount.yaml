{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo22.serviceAccountName" . }}
  labels:
{{ include "foo22.labels" . | indent 4 }}
{{- end -}}
