{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo701.serviceAccountName" . }}
  labels:
{{ include "foo701.labels" . | indent 4 }}
{{- end -}}
