{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo140.serviceAccountName" . }}
  labels:
{{ include "moo140.labels" . | indent 4 }}
{{- end -}}
