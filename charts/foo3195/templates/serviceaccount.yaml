{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo3195.serviceAccountName" . }}
  labels:
{{ include "foo3195.labels" . | indent 4 }}
{{- end -}}
