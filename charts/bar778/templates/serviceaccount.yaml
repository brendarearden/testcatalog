{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar778.serviceAccountName" . }}
  labels:
{{ include "bar778.labels" . | indent 4 }}
{{- end -}}
