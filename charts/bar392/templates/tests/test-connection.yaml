apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar392.fullname" . }}-test-connection"
  labels:
{{ include "bar392.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar392.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
