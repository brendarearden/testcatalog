{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar392.serviceAccountName" . }}
  labels:
{{ include "bar392.labels" . | indent 4 }}
{{- end -}}
