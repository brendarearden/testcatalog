{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo105.serviceAccountName" . }}
  labels:
{{ include "foo105.labels" . | indent 4 }}
{{- end -}}
