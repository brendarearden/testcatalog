{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar956.serviceAccountName" . }}
  labels:
{{ include "bar956.labels" . | indent 4 }}
{{- end -}}
