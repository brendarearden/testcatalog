{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo2002.serviceAccountName" . }}
  labels:
{{ include "foo2002.labels" . | indent 4 }}
{{- end -}}
