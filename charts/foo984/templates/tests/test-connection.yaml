apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo984.fullname" . }}-test-connection"
  labels:
{{ include "foo984.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo984.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
