{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar750.serviceAccountName" . }}
  labels:
{{ include "bar750.labels" . | indent 4 }}
{{- end -}}
