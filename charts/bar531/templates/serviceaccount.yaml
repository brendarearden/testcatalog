{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar531.serviceAccountName" . }}
  labels:
{{ include "bar531.labels" . | indent 4 }}
{{- end -}}
