{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar26.serviceAccountName" . }}
  labels:
{{ include "bar26.labels" . | indent 4 }}
{{- end -}}
