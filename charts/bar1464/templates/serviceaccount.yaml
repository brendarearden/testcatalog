{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1464.serviceAccountName" . }}
  labels:
{{ include "bar1464.labels" . | indent 4 }}
{{- end -}}
