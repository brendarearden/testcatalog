{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1533.serviceAccountName" . }}
  labels:
{{ include "foo1533.labels" . | indent 4 }}
{{- end -}}
