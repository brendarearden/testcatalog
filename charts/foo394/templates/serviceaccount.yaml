{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo394.serviceAccountName" . }}
  labels:
{{ include "foo394.labels" . | indent 4 }}
{{- end -}}
