{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar301.serviceAccountName" . }}
  labels:
{{ include "bar301.labels" . | indent 4 }}
{{- end -}}
