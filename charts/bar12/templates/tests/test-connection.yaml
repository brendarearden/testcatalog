apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar12.fullname" . }}-test-connection"
  labels:
{{ include "bar12.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar12.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
