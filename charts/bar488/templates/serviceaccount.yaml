{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar488.serviceAccountName" . }}
  labels:
{{ include "bar488.labels" . | indent 4 }}
{{- end -}}
