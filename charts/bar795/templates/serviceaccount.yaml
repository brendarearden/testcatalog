{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar795.serviceAccountName" . }}
  labels:
{{ include "bar795.labels" . | indent 4 }}
{{- end -}}
