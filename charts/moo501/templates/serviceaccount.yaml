{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo501.serviceAccountName" . }}
  labels:
{{ include "moo501.labels" . | indent 4 }}
{{- end -}}
