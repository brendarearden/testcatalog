{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar249.serviceAccountName" . }}
  labels:
{{ include "bar249.labels" . | indent 4 }}
{{- end -}}
