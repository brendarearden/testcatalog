{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1447.serviceAccountName" . }}
  labels:
{{ include "foo1447.labels" . | indent 4 }}
{{- end -}}
