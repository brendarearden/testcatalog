{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar877.serviceAccountName" . }}
  labels:
{{ include "bar877.labels" . | indent 4 }}
{{- end -}}
