{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo2364.serviceAccountName" . }}
  labels:
{{ include "foo2364.labels" . | indent 4 }}
{{- end -}}
