{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar426.serviceAccountName" . }}
  labels:
{{ include "bar426.labels" . | indent 4 }}
{{- end -}}
