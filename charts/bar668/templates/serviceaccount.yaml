{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar668.serviceAccountName" . }}
  labels:
{{ include "bar668.labels" . | indent 4 }}
{{- end -}}
