apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo241.fullname" . }}-test-connection"
  labels:
{{ include "foo241.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo241.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
