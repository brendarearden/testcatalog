{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar577.serviceAccountName" . }}
  labels:
{{ include "bar577.labels" . | indent 4 }}
{{- end -}}
