{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo203.serviceAccountName" . }}
  labels:
{{ include "foo203.labels" . | indent 4 }}
{{- end -}}
