{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo501.serviceAccountName" . }}
  labels:
{{ include "foo501.labels" . | indent 4 }}
{{- end -}}
