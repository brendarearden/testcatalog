apiVersion: v1
kind: Service
metadata:
  name: {{ include "bar912.fullname" . }}
  labels:
{{ include "bar912.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "bar912.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
