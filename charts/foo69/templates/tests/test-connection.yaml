apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo69.fullname" . }}-test-connection"
  labels:
{{ include "foo69.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo69.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
