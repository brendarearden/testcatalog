{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar784.serviceAccountName" . }}
  labels:
{{ include "bar784.labels" . | indent 4 }}
{{- end -}}
