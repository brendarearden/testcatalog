apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo196.fullname" . }}-test-connection"
  labels:
{{ include "foo196.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo196.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
