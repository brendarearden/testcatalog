apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo84.fullname" . }}-test-connection"
  labels:
{{ include "moo84.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo84.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
