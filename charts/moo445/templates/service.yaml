apiVersion: v1
kind: Service
metadata:
  name: {{ include "moo445.fullname" . }}
  labels:
{{ include "moo445.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "moo445.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
