apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar159.fullname" . }}-test-connection"
  labels:
{{ include "bar159.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar159.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
