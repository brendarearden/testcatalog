{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo794.serviceAccountName" . }}
  labels:
{{ include "foo794.labels" . | indent 4 }}
{{- end -}}
