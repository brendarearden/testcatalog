{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo3428.serviceAccountName" . }}
  labels:
{{ include "foo3428.labels" . | indent 4 }}
{{- end -}}
