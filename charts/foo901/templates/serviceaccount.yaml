{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo901.serviceAccountName" . }}
  labels:
{{ include "foo901.labels" . | indent 4 }}
{{- end -}}
