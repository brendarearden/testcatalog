{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1885.serviceAccountName" . }}
  labels:
{{ include "bar1885.labels" . | indent 4 }}
{{- end -}}
