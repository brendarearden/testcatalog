{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar975.serviceAccountName" . }}
  labels:
{{ include "bar975.labels" . | indent 4 }}
{{- end -}}
