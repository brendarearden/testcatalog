{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo496.serviceAccountName" . }}
  labels:
{{ include "foo496.labels" . | indent 4 }}
{{- end -}}
