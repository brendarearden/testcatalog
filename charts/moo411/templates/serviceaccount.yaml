{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo411.serviceAccountName" . }}
  labels:
{{ include "moo411.labels" . | indent 4 }}
{{- end -}}
