{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar562.serviceAccountName" . }}
  labels:
{{ include "bar562.labels" . | indent 4 }}
{{- end -}}
