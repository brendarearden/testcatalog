{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo2295.serviceAccountName" . }}
  labels:
{{ include "moo2295.labels" . | indent 4 }}
{{- end -}}
