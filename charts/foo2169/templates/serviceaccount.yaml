{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo2169.serviceAccountName" . }}
  labels:
{{ include "foo2169.labels" . | indent 4 }}
{{- end -}}
