apiVersion: v1
kind: Service
metadata:
  name: {{ include "bar93.fullname" . }}
  labels:
{{ include "bar93.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "bar93.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
