{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1401.serviceAccountName" . }}
  labels:
{{ include "foo1401.labels" . | indent 4 }}
{{- end -}}
