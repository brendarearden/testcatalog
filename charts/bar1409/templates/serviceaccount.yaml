{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1409.serviceAccountName" . }}
  labels:
{{ include "bar1409.labels" . | indent 4 }}
{{- end -}}
