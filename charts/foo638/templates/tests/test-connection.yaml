apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo638.fullname" . }}-test-connection"
  labels:
{{ include "foo638.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo638.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
