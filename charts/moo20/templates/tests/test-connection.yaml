apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo20.fullname" . }}-test-connection"
  labels:
{{ include "moo20.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo20.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
