{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar671.serviceAccountName" . }}
  labels:
{{ include "bar671.labels" . | indent 4 }}
{{- end -}}
