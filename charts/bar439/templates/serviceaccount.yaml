{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar439.serviceAccountName" . }}
  labels:
{{ include "bar439.labels" . | indent 4 }}
{{- end -}}
