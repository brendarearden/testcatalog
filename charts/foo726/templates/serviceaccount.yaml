{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo726.serviceAccountName" . }}
  labels:
{{ include "foo726.labels" . | indent 4 }}
{{- end -}}
