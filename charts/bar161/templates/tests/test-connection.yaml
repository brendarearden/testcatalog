apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar161.fullname" . }}-test-connection"
  labels:
{{ include "bar161.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar161.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
