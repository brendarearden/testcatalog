{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar88.serviceAccountName" . }}
  labels:
{{ include "bar88.labels" . | indent 4 }}
{{- end -}}
