{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1602.serviceAccountName" . }}
  labels:
{{ include "foo1602.labels" . | indent 4 }}
{{- end -}}
