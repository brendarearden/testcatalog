{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar830.serviceAccountName" . }}
  labels:
{{ include "bar830.labels" . | indent 4 }}
{{- end -}}
