{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo697.serviceAccountName" . }}
  labels:
{{ include "foo697.labels" . | indent 4 }}
{{- end -}}
