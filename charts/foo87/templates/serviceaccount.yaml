{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo87.serviceAccountName" . }}
  labels:
{{ include "foo87.labels" . | indent 4 }}
{{- end -}}
