{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar876.serviceAccountName" . }}
  labels:
{{ include "bar876.labels" . | indent 4 }}
{{- end -}}
