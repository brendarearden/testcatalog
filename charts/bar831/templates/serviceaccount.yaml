{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar831.serviceAccountName" . }}
  labels:
{{ include "bar831.labels" . | indent 4 }}
{{- end -}}
