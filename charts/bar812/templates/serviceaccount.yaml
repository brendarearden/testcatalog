{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar812.serviceAccountName" . }}
  labels:
{{ include "bar812.labels" . | indent 4 }}
{{- end -}}
