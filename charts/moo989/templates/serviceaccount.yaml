{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo989.serviceAccountName" . }}
  labels:
{{ include "moo989.labels" . | indent 4 }}
{{- end -}}
