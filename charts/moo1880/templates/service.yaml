apiVersion: v1
kind: Service
metadata:
  name: {{ include "moo1880.fullname" . }}
  labels:
{{ include "moo1880.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "moo1880.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
