apiVersion: v1
kind: Service
metadata:
  name: {{ include "moo841.fullname" . }}
  labels:
{{ include "moo841.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "moo841.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
