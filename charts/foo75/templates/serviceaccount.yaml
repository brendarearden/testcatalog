{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo75.serviceAccountName" . }}
  labels:
{{ include "foo75.labels" . | indent 4 }}
{{- end -}}
