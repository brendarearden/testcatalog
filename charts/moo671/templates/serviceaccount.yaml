{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo671.serviceAccountName" . }}
  labels:
{{ include "moo671.labels" . | indent 4 }}
{{- end -}}
