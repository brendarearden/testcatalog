{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1988.serviceAccountName" . }}
  labels:
{{ include "foo1988.labels" . | indent 4 }}
{{- end -}}
