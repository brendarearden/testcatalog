apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo391.fullname" . }}-test-connection"
  labels:
{{ include "moo391.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo391.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
