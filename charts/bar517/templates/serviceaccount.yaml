{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar517.serviceAccountName" . }}
  labels:
{{ include "bar517.labels" . | indent 4 }}
{{- end -}}
