apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo3627.fullname" . }}-test-connection"
  labels:
{{ include "moo3627.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo3627.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
