{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar810.serviceAccountName" . }}
  labels:
{{ include "bar810.labels" . | indent 4 }}
{{- end -}}
