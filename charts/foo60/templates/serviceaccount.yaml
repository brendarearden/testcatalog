{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo60.serviceAccountName" . }}
  labels:
{{ include "foo60.labels" . | indent 4 }}
{{- end -}}
