{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo455.serviceAccountName" . }}
  labels:
{{ include "foo455.labels" . | indent 4 }}
{{- end -}}
