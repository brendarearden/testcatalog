apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo549.fullname" . }}-test-connection"
  labels:
{{ include "foo549.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo549.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
