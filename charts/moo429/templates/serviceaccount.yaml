{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo429.serviceAccountName" . }}
  labels:
{{ include "moo429.labels" . | indent 4 }}
{{- end -}}
