apiVersion: v1
kind: Service
metadata:
  name: {{ include "moo526.fullname" . }}
  labels:
{{ include "moo526.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "moo526.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
