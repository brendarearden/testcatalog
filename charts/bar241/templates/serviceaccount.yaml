{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar241.serviceAccountName" . }}
  labels:
{{ include "bar241.labels" . | indent 4 }}
{{- end -}}
