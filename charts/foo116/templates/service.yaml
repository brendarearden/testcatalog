apiVersion: v1
kind: Service
metadata:
  name: {{ include "foo116.fullname" . }}
  labels:
{{ include "foo116.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "foo116.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
