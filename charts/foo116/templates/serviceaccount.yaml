{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo116.serviceAccountName" . }}
  labels:
{{ include "foo116.labels" . | indent 4 }}
{{- end -}}
