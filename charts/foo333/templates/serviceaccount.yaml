{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo333.serviceAccountName" . }}
  labels:
{{ include "foo333.labels" . | indent 4 }}
{{- end -}}
