apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo855.fullname" . }}-test-connection"
  labels:
{{ include "moo855.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo855.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
