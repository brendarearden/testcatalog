{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo655.serviceAccountName" . }}
  labels:
{{ include "foo655.labels" . | indent 4 }}
{{- end -}}
