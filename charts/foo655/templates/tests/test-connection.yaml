apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo655.fullname" . }}-test-connection"
  labels:
{{ include "foo655.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo655.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
