apiVersion: v1
kind: Service
metadata:
  name: {{ include "bar284.fullname" . }}
  labels:
{{ include "bar284.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "bar284.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
