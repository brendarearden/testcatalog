apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar1881.fullname" . }}-test-connection"
  labels:
{{ include "bar1881.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar1881.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
