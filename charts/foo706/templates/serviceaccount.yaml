{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo706.serviceAccountName" . }}
  labels:
{{ include "foo706.labels" . | indent 4 }}
{{- end -}}
