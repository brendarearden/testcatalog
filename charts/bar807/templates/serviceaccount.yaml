{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar807.serviceAccountName" . }}
  labels:
{{ include "bar807.labels" . | indent 4 }}
{{- end -}}
