{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo423.serviceAccountName" . }}
  labels:
{{ include "foo423.labels" . | indent 4 }}
{{- end -}}
