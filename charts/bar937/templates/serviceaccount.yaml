{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar937.serviceAccountName" . }}
  labels:
{{ include "bar937.labels" . | indent 4 }}
{{- end -}}
