{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo671.serviceAccountName" . }}
  labels:
{{ include "foo671.labels" . | indent 4 }}
{{- end -}}
