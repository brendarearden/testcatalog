{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1058.serviceAccountName" . }}
  labels:
{{ include "bar1058.labels" . | indent 4 }}
{{- end -}}
