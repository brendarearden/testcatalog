{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo387.serviceAccountName" . }}
  labels:
{{ include "foo387.labels" . | indent 4 }}
{{- end -}}
