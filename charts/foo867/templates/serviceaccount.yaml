{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo867.serviceAccountName" . }}
  labels:
{{ include "foo867.labels" . | indent 4 }}
{{- end -}}
