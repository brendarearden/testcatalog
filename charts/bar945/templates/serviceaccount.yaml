{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar945.serviceAccountName" . }}
  labels:
{{ include "bar945.labels" . | indent 4 }}
{{- end -}}
