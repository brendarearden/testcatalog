{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo15.serviceAccountName" . }}
  labels:
{{ include "moo15.labels" . | indent 4 }}
{{- end -}}
