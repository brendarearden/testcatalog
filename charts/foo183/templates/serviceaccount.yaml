{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo183.serviceAccountName" . }}
  labels:
{{ include "foo183.labels" . | indent 4 }}
{{- end -}}
