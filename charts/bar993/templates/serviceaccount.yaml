{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar993.serviceAccountName" . }}
  labels:
{{ include "bar993.labels" . | indent 4 }}
{{- end -}}
