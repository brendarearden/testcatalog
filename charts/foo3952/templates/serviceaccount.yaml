{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo3952.serviceAccountName" . }}
  labels:
{{ include "foo3952.labels" . | indent 4 }}
{{- end -}}
