{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar536.serviceAccountName" . }}
  labels:
{{ include "bar536.labels" . | indent 4 }}
{{- end -}}
