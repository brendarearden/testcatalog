{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo988.serviceAccountName" . }}
  labels:
{{ include "foo988.labels" . | indent 4 }}
{{- end -}}
