{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo2451.serviceAccountName" . }}
  labels:
{{ include "foo2451.labels" . | indent 4 }}
{{- end -}}
