{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo3143.serviceAccountName" . }}
  labels:
{{ include "foo3143.labels" . | indent 4 }}
{{- end -}}
