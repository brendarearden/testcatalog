{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1537.serviceAccountName" . }}
  labels:
{{ include "bar1537.labels" . | indent 4 }}
{{- end -}}
