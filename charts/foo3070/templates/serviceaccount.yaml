{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo3070.serviceAccountName" . }}
  labels:
{{ include "foo3070.labels" . | indent 4 }}
{{- end -}}
