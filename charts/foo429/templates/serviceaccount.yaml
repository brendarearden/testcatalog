{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo429.serviceAccountName" . }}
  labels:
{{ include "foo429.labels" . | indent 4 }}
{{- end -}}
