{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo555.serviceAccountName" . }}
  labels:
{{ include "foo555.labels" . | indent 4 }}
{{- end -}}
