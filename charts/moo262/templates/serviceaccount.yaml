{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo262.serviceAccountName" . }}
  labels:
{{ include "moo262.labels" . | indent 4 }}
{{- end -}}
