{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo2420.serviceAccountName" . }}
  labels:
{{ include "foo2420.labels" . | indent 4 }}
{{- end -}}
