{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo243.serviceAccountName" . }}
  labels:
{{ include "foo243.labels" . | indent 4 }}
{{- end -}}
