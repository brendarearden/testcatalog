{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar201.serviceAccountName" . }}
  labels:
{{ include "bar201.labels" . | indent 4 }}
{{- end -}}
