{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo958.serviceAccountName" . }}
  labels:
{{ include "foo958.labels" . | indent 4 }}
{{- end -}}
