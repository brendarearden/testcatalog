{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo599.serviceAccountName" . }}
  labels:
{{ include "foo599.labels" . | indent 4 }}
{{- end -}}
