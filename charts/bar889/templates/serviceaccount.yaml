{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar889.serviceAccountName" . }}
  labels:
{{ include "bar889.labels" . | indent 4 }}
{{- end -}}
