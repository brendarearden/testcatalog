{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo456.serviceAccountName" . }}
  labels:
{{ include "foo456.labels" . | indent 4 }}
{{- end -}}
