{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo2667.serviceAccountName" . }}
  labels:
{{ include "foo2667.labels" . | indent 4 }}
{{- end -}}
