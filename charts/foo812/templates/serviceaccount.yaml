{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo812.serviceAccountName" . }}
  labels:
{{ include "foo812.labels" . | indent 4 }}
{{- end -}}
