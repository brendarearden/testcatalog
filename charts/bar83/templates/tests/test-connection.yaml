apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar83.fullname" . }}-test-connection"
  labels:
{{ include "bar83.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar83.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
