{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar83.serviceAccountName" . }}
  labels:
{{ include "bar83.labels" . | indent 4 }}
{{- end -}}
