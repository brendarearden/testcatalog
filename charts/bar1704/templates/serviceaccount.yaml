{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1704.serviceAccountName" . }}
  labels:
{{ include "bar1704.labels" . | indent 4 }}
{{- end -}}
