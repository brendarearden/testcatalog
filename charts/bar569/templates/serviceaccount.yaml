{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar569.serviceAccountName" . }}
  labels:
{{ include "bar569.labels" . | indent 4 }}
{{- end -}}
