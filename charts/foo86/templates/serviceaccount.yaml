{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo86.serviceAccountName" . }}
  labels:
{{ include "foo86.labels" . | indent 4 }}
{{- end -}}
