{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo2203.serviceAccountName" . }}
  labels:
{{ include "moo2203.labels" . | indent 4 }}
{{- end -}}
