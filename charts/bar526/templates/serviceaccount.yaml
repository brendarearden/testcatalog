{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar526.serviceAccountName" . }}
  labels:
{{ include "bar526.labels" . | indent 4 }}
{{- end -}}
