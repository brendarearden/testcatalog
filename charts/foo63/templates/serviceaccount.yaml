{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo63.serviceAccountName" . }}
  labels:
{{ include "foo63.labels" . | indent 4 }}
{{- end -}}
