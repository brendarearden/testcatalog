{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo1300.serviceAccountName" . }}
  labels:
{{ include "moo1300.labels" . | indent 4 }}
{{- end -}}
