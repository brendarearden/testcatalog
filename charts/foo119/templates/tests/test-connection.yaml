apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo119.fullname" . }}-test-connection"
  labels:
{{ include "foo119.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo119.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
