{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo885.serviceAccountName" . }}
  labels:
{{ include "foo885.labels" . | indent 4 }}
{{- end -}}
