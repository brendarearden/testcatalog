{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo530.serviceAccountName" . }}
  labels:
{{ include "foo530.labels" . | indent 4 }}
{{- end -}}
