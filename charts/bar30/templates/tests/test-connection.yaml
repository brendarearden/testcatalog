apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar30.fullname" . }}-test-connection"
  labels:
{{ include "bar30.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar30.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
