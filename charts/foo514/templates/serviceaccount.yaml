{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo514.serviceAccountName" . }}
  labels:
{{ include "foo514.labels" . | indent 4 }}
{{- end -}}
