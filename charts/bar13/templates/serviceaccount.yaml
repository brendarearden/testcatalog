{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar13.serviceAccountName" . }}
  labels:
{{ include "bar13.labels" . | indent 4 }}
{{- end -}}
