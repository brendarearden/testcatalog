{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo3241.serviceAccountName" . }}
  labels:
{{ include "foo3241.labels" . | indent 4 }}
{{- end -}}
