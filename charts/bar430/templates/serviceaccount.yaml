{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar430.serviceAccountName" . }}
  labels:
{{ include "bar430.labels" . | indent 4 }}
{{- end -}}
