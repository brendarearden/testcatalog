apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo163.fullname" . }}-test-connection"
  labels:
{{ include "foo163.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo163.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
