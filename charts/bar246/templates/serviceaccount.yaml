{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar246.serviceAccountName" . }}
  labels:
{{ include "bar246.labels" . | indent 4 }}
{{- end -}}
