{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo876.serviceAccountName" . }}
  labels:
{{ include "foo876.labels" . | indent 4 }}
{{- end -}}
