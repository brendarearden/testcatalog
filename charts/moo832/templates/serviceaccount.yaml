{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo832.serviceAccountName" . }}
  labels:
{{ include "moo832.labels" . | indent 4 }}
{{- end -}}
