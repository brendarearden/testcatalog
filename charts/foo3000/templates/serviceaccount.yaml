{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo3000.serviceAccountName" . }}
  labels:
{{ include "foo3000.labels" . | indent 4 }}
{{- end -}}
