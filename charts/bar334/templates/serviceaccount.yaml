{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar334.serviceAccountName" . }}
  labels:
{{ include "bar334.labels" . | indent 4 }}
{{- end -}}
