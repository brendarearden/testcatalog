apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar14.fullname" . }}-test-connection"
  labels:
{{ include "bar14.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar14.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
