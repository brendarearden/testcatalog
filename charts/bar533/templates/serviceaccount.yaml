{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar533.serviceAccountName" . }}
  labels:
{{ include "bar533.labels" . | indent 4 }}
{{- end -}}
