apiVersion: v1
kind: Service
metadata:
  name: {{ include "bar533.fullname" . }}
  labels:
{{ include "bar533.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "bar533.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
