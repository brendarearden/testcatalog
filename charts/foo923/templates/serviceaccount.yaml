{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo923.serviceAccountName" . }}
  labels:
{{ include "foo923.labels" . | indent 4 }}
{{- end -}}
