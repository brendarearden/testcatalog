{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1692.serviceAccountName" . }}
  labels:
{{ include "bar1692.labels" . | indent 4 }}
{{- end -}}
