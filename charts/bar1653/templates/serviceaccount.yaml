{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1653.serviceAccountName" . }}
  labels:
{{ include "bar1653.labels" . | indent 4 }}
{{- end -}}
