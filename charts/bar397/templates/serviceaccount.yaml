{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar397.serviceAccountName" . }}
  labels:
{{ include "bar397.labels" . | indent 4 }}
{{- end -}}
