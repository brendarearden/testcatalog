{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar173.serviceAccountName" . }}
  labels:
{{ include "bar173.labels" . | indent 4 }}
{{- end -}}
