{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1171.serviceAccountName" . }}
  labels:
{{ include "bar1171.labels" . | indent 4 }}
{{- end -}}
