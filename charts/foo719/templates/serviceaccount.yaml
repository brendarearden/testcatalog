{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo719.serviceAccountName" . }}
  labels:
{{ include "foo719.labels" . | indent 4 }}
{{- end -}}
