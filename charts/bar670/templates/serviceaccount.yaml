{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar670.serviceAccountName" . }}
  labels:
{{ include "bar670.labels" . | indent 4 }}
{{- end -}}
