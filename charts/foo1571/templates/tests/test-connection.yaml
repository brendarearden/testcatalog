apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo1571.fullname" . }}-test-connection"
  labels:
{{ include "foo1571.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo1571.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
