{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo432.serviceAccountName" . }}
  labels:
{{ include "foo432.labels" . | indent 4 }}
{{- end -}}
