{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo2418.serviceAccountName" . }}
  labels:
{{ include "moo2418.labels" . | indent 4 }}
{{- end -}}
