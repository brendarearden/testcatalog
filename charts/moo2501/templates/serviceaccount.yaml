{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo2501.serviceAccountName" . }}
  labels:
{{ include "moo2501.labels" . | indent 4 }}
{{- end -}}
