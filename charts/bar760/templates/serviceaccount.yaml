{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar760.serviceAccountName" . }}
  labels:
{{ include "bar760.labels" . | indent 4 }}
{{- end -}}
