apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo48.fullname" . }}-test-connection"
  labels:
{{ include "moo48.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo48.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
