{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar958.serviceAccountName" . }}
  labels:
{{ include "bar958.labels" . | indent 4 }}
{{- end -}}
