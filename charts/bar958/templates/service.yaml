apiVersion: v1
kind: Service
metadata:
  name: {{ include "bar958.fullname" . }}
  labels:
{{ include "bar958.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "bar958.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
