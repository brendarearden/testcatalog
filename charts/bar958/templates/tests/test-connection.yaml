apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar958.fullname" . }}-test-connection"
  labels:
{{ include "bar958.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar958.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
