apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar995.fullname" . }}-test-connection"
  labels:
{{ include "bar995.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar995.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
