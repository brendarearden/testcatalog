apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo2327.fullname" . }}-test-connection"
  labels:
{{ include "foo2327.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo2327.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
