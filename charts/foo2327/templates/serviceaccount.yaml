{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo2327.serviceAccountName" . }}
  labels:
{{ include "foo2327.labels" . | indent 4 }}
{{- end -}}
