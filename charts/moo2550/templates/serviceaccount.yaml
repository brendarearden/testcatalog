{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo2550.serviceAccountName" . }}
  labels:
{{ include "moo2550.labels" . | indent 4 }}
{{- end -}}
