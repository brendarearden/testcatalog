apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar1782.fullname" . }}-test-connection"
  labels:
{{ include "bar1782.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar1782.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
