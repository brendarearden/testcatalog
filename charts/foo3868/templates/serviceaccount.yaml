{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo3868.serviceAccountName" . }}
  labels:
{{ include "foo3868.labels" . | indent 4 }}
{{- end -}}
