{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1080.serviceAccountName" . }}
  labels:
{{ include "bar1080.labels" . | indent 4 }}
{{- end -}}
