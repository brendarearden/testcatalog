{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo271.serviceAccountName" . }}
  labels:
{{ include "foo271.labels" . | indent 4 }}
{{- end -}}
