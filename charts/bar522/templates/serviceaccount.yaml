{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar522.serviceAccountName" . }}
  labels:
{{ include "bar522.labels" . | indent 4 }}
{{- end -}}
