{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo892.serviceAccountName" . }}
  labels:
{{ include "foo892.labels" . | indent 4 }}
{{- end -}}
