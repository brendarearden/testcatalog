apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo2666.fullname" . }}-test-connection"
  labels:
{{ include "moo2666.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo2666.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
