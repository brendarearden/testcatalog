{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1170.serviceAccountName" . }}
  labels:
{{ include "bar1170.labels" . | indent 4 }}
{{- end -}}
