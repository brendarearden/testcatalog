{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar850.serviceAccountName" . }}
  labels:
{{ include "bar850.labels" . | indent 4 }}
{{- end -}}
