{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar620.serviceAccountName" . }}
  labels:
{{ include "bar620.labels" . | indent 4 }}
{{- end -}}
