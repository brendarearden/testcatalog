{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1109.serviceAccountName" . }}
  labels:
{{ include "foo1109.labels" . | indent 4 }}
{{- end -}}
