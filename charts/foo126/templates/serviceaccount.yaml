{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo126.serviceAccountName" . }}
  labels:
{{ include "foo126.labels" . | indent 4 }}
{{- end -}}
