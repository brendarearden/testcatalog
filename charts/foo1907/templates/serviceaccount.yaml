{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1907.serviceAccountName" . }}
  labels:
{{ include "foo1907.labels" . | indent 4 }}
{{- end -}}
