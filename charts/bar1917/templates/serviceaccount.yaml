{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1917.serviceAccountName" . }}
  labels:
{{ include "bar1917.labels" . | indent 4 }}
{{- end -}}
