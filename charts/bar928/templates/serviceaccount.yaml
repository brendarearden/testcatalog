{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar928.serviceAccountName" . }}
  labels:
{{ include "bar928.labels" . | indent 4 }}
{{- end -}}
