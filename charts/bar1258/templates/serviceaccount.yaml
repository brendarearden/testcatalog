{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1258.serviceAccountName" . }}
  labels:
{{ include "bar1258.labels" . | indent 4 }}
{{- end -}}
