{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar800.serviceAccountName" . }}
  labels:
{{ include "bar800.labels" . | indent 4 }}
{{- end -}}
