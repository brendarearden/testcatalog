{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar523.serviceAccountName" . }}
  labels:
{{ include "bar523.labels" . | indent 4 }}
{{- end -}}
