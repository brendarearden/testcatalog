apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar702.fullname" . }}-test-connection"
  labels:
{{ include "bar702.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar702.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
