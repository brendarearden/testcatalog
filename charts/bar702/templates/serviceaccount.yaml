{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar702.serviceAccountName" . }}
  labels:
{{ include "bar702.labels" . | indent 4 }}
{{- end -}}
