{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo654.serviceAccountName" . }}
  labels:
{{ include "moo654.labels" . | indent 4 }}
{{- end -}}
