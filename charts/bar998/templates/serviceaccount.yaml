{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar998.serviceAccountName" . }}
  labels:
{{ include "bar998.labels" . | indent 4 }}
{{- end -}}
