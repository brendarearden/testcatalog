{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo678.serviceAccountName" . }}
  labels:
{{ include "foo678.labels" . | indent 4 }}
{{- end -}}
