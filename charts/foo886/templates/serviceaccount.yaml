{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo886.serviceAccountName" . }}
  labels:
{{ include "foo886.labels" . | indent 4 }}
{{- end -}}
