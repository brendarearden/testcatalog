{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar698.serviceAccountName" . }}
  labels:
{{ include "bar698.labels" . | indent 4 }}
{{- end -}}
