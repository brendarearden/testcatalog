{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1350.serviceAccountName" . }}
  labels:
{{ include "bar1350.labels" . | indent 4 }}
{{- end -}}
