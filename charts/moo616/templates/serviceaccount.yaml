{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo616.serviceAccountName" . }}
  labels:
{{ include "moo616.labels" . | indent 4 }}
{{- end -}}
