apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo718.fullname" . }}-test-connection"
  labels:
{{ include "moo718.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo718.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
