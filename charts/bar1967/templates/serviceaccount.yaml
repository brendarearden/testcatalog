{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1967.serviceAccountName" . }}
  labels:
{{ include "bar1967.labels" . | indent 4 }}
{{- end -}}
