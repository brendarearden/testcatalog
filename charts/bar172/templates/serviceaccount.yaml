{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar172.serviceAccountName" . }}
  labels:
{{ include "bar172.labels" . | indent 4 }}
{{- end -}}
