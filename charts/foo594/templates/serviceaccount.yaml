{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo594.serviceAccountName" . }}
  labels:
{{ include "foo594.labels" . | indent 4 }}
{{- end -}}
