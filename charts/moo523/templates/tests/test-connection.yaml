apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo523.fullname" . }}-test-connection"
  labels:
{{ include "moo523.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo523.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
