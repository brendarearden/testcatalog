{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar774.serviceAccountName" . }}
  labels:
{{ include "bar774.labels" . | indent 4 }}
{{- end -}}
