{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo484.serviceAccountName" . }}
  labels:
{{ include "foo484.labels" . | indent 4 }}
{{- end -}}
