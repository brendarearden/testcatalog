{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo400.serviceAccountName" . }}
  labels:
{{ include "foo400.labels" . | indent 4 }}
{{- end -}}
