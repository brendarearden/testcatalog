{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar279.serviceAccountName" . }}
  labels:
{{ include "bar279.labels" . | indent 4 }}
{{- end -}}
