apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar429.fullname" . }}-test-connection"
  labels:
{{ include "bar429.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar429.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
