{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar429.serviceAccountName" . }}
  labels:
{{ include "bar429.labels" . | indent 4 }}
{{- end -}}
