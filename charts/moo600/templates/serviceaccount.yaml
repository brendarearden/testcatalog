{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo600.serviceAccountName" . }}
  labels:
{{ include "moo600.labels" . | indent 4 }}
{{- end -}}
