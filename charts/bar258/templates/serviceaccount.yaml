{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar258.serviceAccountName" . }}
  labels:
{{ include "bar258.labels" . | indent 4 }}
{{- end -}}
