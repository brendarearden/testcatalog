{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar142.serviceAccountName" . }}
  labels:
{{ include "bar142.labels" . | indent 4 }}
{{- end -}}
