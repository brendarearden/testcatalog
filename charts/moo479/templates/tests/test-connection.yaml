apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo479.fullname" . }}-test-connection"
  labels:
{{ include "moo479.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo479.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
