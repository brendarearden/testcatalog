apiVersion: v1
kind: Service
metadata:
  name: {{ include "moo479.fullname" . }}
  labels:
{{ include "moo479.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "moo479.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
