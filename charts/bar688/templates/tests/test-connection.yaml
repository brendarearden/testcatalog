apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar688.fullname" . }}-test-connection"
  labels:
{{ include "bar688.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar688.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
