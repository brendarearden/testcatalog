{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar688.serviceAccountName" . }}
  labels:
{{ include "bar688.labels" . | indent 4 }}
{{- end -}}
