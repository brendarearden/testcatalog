{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar673.serviceAccountName" . }}
  labels:
{{ include "bar673.labels" . | indent 4 }}
{{- end -}}
