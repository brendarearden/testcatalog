apiVersion: v1
kind: Service
metadata:
  name: {{ include "moo102.fullname" . }}
  labels:
{{ include "moo102.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "moo102.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
