{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo102.serviceAccountName" . }}
  labels:
{{ include "moo102.labels" . | indent 4 }}
{{- end -}}
