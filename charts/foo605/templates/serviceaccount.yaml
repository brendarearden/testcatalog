{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo605.serviceAccountName" . }}
  labels:
{{ include "foo605.labels" . | indent 4 }}
{{- end -}}
