{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar414.serviceAccountName" . }}
  labels:
{{ include "bar414.labels" . | indent 4 }}
{{- end -}}
