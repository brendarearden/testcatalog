{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar379.serviceAccountName" . }}
  labels:
{{ include "bar379.labels" . | indent 4 }}
{{- end -}}
