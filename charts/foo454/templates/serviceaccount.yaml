{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo454.serviceAccountName" . }}
  labels:
{{ include "foo454.labels" . | indent 4 }}
{{- end -}}
