apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo766.fullname" . }}-test-connection"
  labels:
{{ include "moo766.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo766.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
