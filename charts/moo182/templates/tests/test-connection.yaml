apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo182.fullname" . }}-test-connection"
  labels:
{{ include "moo182.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo182.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
