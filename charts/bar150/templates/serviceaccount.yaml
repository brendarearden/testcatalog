{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar150.serviceAccountName" . }}
  labels:
{{ include "bar150.labels" . | indent 4 }}
{{- end -}}
