{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1656.serviceAccountName" . }}
  labels:
{{ include "foo1656.labels" . | indent 4 }}
{{- end -}}
