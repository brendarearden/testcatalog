{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar759.serviceAccountName" . }}
  labels:
{{ include "bar759.labels" . | indent 4 }}
{{- end -}}
