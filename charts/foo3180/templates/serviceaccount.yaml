{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo3180.serviceAccountName" . }}
  labels:
{{ include "foo3180.labels" . | indent 4 }}
{{- end -}}
