{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo91.serviceAccountName" . }}
  labels:
{{ include "foo91.labels" . | indent 4 }}
{{- end -}}
