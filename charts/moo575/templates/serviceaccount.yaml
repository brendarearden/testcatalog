{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo575.serviceAccountName" . }}
  labels:
{{ include "moo575.labels" . | indent 4 }}
{{- end -}}
