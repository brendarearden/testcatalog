{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar660.serviceAccountName" . }}
  labels:
{{ include "bar660.labels" . | indent 4 }}
{{- end -}}
