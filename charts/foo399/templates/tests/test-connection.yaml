apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo399.fullname" . }}-test-connection"
  labels:
{{ include "foo399.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo399.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
