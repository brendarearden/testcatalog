apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo480.fullname" . }}-test-connection"
  labels:
{{ include "foo480.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo480.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
