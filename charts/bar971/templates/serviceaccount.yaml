{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar971.serviceAccountName" . }}
  labels:
{{ include "bar971.labels" . | indent 4 }}
{{- end -}}
