{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo244.serviceAccountName" . }}
  labels:
{{ include "foo244.labels" . | indent 4 }}
{{- end -}}
