{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo381.serviceAccountName" . }}
  labels:
{{ include "moo381.labels" . | indent 4 }}
{{- end -}}
