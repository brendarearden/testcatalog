{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1968.serviceAccountName" . }}
  labels:
{{ include "foo1968.labels" . | indent 4 }}
{{- end -}}
