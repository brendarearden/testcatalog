{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1710.serviceAccountName" . }}
  labels:
{{ include "bar1710.labels" . | indent 4 }}
{{- end -}}
