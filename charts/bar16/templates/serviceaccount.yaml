{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar16.serviceAccountName" . }}
  labels:
{{ include "bar16.labels" . | indent 4 }}
{{- end -}}
