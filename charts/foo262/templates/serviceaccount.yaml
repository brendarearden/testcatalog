{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo262.serviceAccountName" . }}
  labels:
{{ include "foo262.labels" . | indent 4 }}
{{- end -}}
