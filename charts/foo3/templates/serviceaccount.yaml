{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo3.serviceAccountName" . }}
  labels:
{{ include "foo3.labels" . | indent 4 }}
{{- end -}}
