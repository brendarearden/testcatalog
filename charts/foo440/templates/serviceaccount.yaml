{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo440.serviceAccountName" . }}
  labels:
{{ include "foo440.labels" . | indent 4 }}
{{- end -}}
