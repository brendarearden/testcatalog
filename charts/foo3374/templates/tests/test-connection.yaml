apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo3374.fullname" . }}-test-connection"
  labels:
{{ include "foo3374.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo3374.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
