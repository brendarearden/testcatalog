{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar955.serviceAccountName" . }}
  labels:
{{ include "bar955.labels" . | indent 4 }}
{{- end -}}
