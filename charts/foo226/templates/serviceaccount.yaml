{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo226.serviceAccountName" . }}
  labels:
{{ include "foo226.labels" . | indent 4 }}
{{- end -}}
