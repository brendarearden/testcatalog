apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo634.fullname" . }}-test-connection"
  labels:
{{ include "moo634.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo634.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
