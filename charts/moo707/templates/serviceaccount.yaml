{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo707.serviceAccountName" . }}
  labels:
{{ include "moo707.labels" . | indent 4 }}
{{- end -}}
