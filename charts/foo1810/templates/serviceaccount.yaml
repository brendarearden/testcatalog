{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1810.serviceAccountName" . }}
  labels:
{{ include "foo1810.labels" . | indent 4 }}
{{- end -}}
