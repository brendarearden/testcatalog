{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1715.serviceAccountName" . }}
  labels:
{{ include "bar1715.labels" . | indent 4 }}
{{- end -}}
