{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar283.serviceAccountName" . }}
  labels:
{{ include "bar283.labels" . | indent 4 }}
{{- end -}}
