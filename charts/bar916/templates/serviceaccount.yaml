{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar916.serviceAccountName" . }}
  labels:
{{ include "bar916.labels" . | indent 4 }}
{{- end -}}
