apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bar490.fullname" . }}-test-connection"
  labels:
{{ include "bar490.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "bar490.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
