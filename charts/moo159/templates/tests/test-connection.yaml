apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo159.fullname" . }}-test-connection"
  labels:
{{ include "moo159.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo159.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
