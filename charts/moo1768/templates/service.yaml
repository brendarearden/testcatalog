apiVersion: v1
kind: Service
metadata:
  name: {{ include "moo1768.fullname" . }}
  labels:
{{ include "moo1768.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "moo1768.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
