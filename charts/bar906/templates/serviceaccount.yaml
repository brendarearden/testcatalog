{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar906.serviceAccountName" . }}
  labels:
{{ include "bar906.labels" . | indent 4 }}
{{- end -}}
