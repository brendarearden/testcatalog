{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo523.serviceAccountName" . }}
  labels:
{{ include "foo523.labels" . | indent 4 }}
{{- end -}}
