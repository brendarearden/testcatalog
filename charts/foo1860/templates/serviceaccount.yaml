{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo1860.serviceAccountName" . }}
  labels:
{{ include "foo1860.labels" . | indent 4 }}
{{- end -}}
