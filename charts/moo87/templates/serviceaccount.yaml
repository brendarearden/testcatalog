{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo87.serviceAccountName" . }}
  labels:
{{ include "moo87.labels" . | indent 4 }}
{{- end -}}
