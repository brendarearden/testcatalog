{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1827.serviceAccountName" . }}
  labels:
{{ include "bar1827.labels" . | indent 4 }}
{{- end -}}
