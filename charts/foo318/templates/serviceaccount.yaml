{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo318.serviceAccountName" . }}
  labels:
{{ include "foo318.labels" . | indent 4 }}
{{- end -}}
