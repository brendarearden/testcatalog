apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo1540.fullname" . }}-test-connection"
  labels:
{{ include "foo1540.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo1540.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
