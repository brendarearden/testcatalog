apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo24.fullname" . }}-test-connection"
  labels:
{{ include "moo24.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo24.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
