{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1097.serviceAccountName" . }}
  labels:
{{ include "bar1097.labels" . | indent 4 }}
{{- end -}}
