{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar229.serviceAccountName" . }}
  labels:
{{ include "bar229.labels" . | indent 4 }}
{{- end -}}
