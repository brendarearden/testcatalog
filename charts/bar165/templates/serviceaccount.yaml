{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar165.serviceAccountName" . }}
  labels:
{{ include "bar165.labels" . | indent 4 }}
{{- end -}}
