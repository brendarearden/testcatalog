{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar251.serviceAccountName" . }}
  labels:
{{ include "bar251.labels" . | indent 4 }}
{{- end -}}
