{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo380.serviceAccountName" . }}
  labels:
{{ include "foo380.labels" . | indent 4 }}
{{- end -}}
