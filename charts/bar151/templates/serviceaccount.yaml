{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar151.serviceAccountName" . }}
  labels:
{{ include "bar151.labels" . | indent 4 }}
{{- end -}}
