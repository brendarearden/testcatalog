apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo1889.fullname" . }}-test-connection"
  labels:
{{ include "foo1889.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo1889.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
