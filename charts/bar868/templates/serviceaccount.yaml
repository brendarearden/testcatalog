{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar868.serviceAccountName" . }}
  labels:
{{ include "bar868.labels" . | indent 4 }}
{{- end -}}
