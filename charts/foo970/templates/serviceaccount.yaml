{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo970.serviceAccountName" . }}
  labels:
{{ include "foo970.labels" . | indent 4 }}
{{- end -}}
