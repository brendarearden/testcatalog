apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo77.fullname" . }}-test-connection"
  labels:
{{ include "moo77.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo77.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
