{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo422.serviceAccountName" . }}
  labels:
{{ include "foo422.labels" . | indent 4 }}
{{- end -}}
