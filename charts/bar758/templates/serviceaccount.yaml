{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar758.serviceAccountName" . }}
  labels:
{{ include "bar758.labels" . | indent 4 }}
{{- end -}}
