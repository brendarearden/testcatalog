apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo662.fullname" . }}-test-connection"
  labels:
{{ include "moo662.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo662.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
