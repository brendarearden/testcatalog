{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar9.serviceAccountName" . }}
  labels:
{{ include "bar9.labels" . | indent 4 }}
{{- end -}}
