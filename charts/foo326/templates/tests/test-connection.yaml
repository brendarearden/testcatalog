apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo326.fullname" . }}-test-connection"
  labels:
{{ include "foo326.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo326.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
