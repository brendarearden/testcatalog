{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo2638.serviceAccountName" . }}
  labels:
{{ include "moo2638.labels" . | indent 4 }}
{{- end -}}
