{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1543.serviceAccountName" . }}
  labels:
{{ include "bar1543.labels" . | indent 4 }}
{{- end -}}
