apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo749.fullname" . }}-test-connection"
  labels:
{{ include "moo749.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo749.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
