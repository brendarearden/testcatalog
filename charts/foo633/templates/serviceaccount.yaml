{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo633.serviceAccountName" . }}
  labels:
{{ include "foo633.labels" . | indent 4 }}
{{- end -}}
