{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo274.serviceAccountName" . }}
  labels:
{{ include "foo274.labels" . | indent 4 }}
{{- end -}}
