{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar463.serviceAccountName" . }}
  labels:
{{ include "bar463.labels" . | indent 4 }}
{{- end -}}
