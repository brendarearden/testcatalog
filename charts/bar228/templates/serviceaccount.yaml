{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar228.serviceAccountName" . }}
  labels:
{{ include "bar228.labels" . | indent 4 }}
{{- end -}}
