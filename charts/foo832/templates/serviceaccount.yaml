{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo832.serviceAccountName" . }}
  labels:
{{ include "foo832.labels" . | indent 4 }}
{{- end -}}
