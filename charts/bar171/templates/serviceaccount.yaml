{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar171.serviceAccountName" . }}
  labels:
{{ include "bar171.labels" . | indent 4 }}
{{- end -}}
