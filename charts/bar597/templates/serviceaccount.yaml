{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar597.serviceAccountName" . }}
  labels:
{{ include "bar597.labels" . | indent 4 }}
{{- end -}}
