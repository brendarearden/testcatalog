apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moo1725.fullname" . }}-test-connection"
  labels:
{{ include "moo1725.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "moo1725.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
