{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo342.serviceAccountName" . }}
  labels:
{{ include "foo342.labels" . | indent 4 }}
{{- end -}}
