{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar789.serviceAccountName" . }}
  labels:
{{ include "bar789.labels" . | indent 4 }}
{{- end -}}
