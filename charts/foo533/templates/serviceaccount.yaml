{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo533.serviceAccountName" . }}
  labels:
{{ include "foo533.labels" . | indent 4 }}
{{- end -}}
