{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1858.serviceAccountName" . }}
  labels:
{{ include "bar1858.labels" . | indent 4 }}
{{- end -}}
