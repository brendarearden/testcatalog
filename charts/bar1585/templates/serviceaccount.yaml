{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar1585.serviceAccountName" . }}
  labels:
{{ include "bar1585.labels" . | indent 4 }}
{{- end -}}
