{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo405.serviceAccountName" . }}
  labels:
{{ include "foo405.labels" . | indent 4 }}
{{- end -}}
