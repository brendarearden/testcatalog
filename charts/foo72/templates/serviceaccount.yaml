{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "foo72.serviceAccountName" . }}
  labels:
{{ include "foo72.labels" . | indent 4 }}
{{- end -}}
