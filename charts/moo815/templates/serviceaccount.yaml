{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo815.serviceAccountName" . }}
  labels:
{{ include "moo815.labels" . | indent 4 }}
{{- end -}}
