apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo70.fullname" . }}-test-connection"
  labels:
{{ include "foo70.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "foo70.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
