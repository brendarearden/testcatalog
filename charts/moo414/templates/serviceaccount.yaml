{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo414.serviceAccountName" . }}
  labels:
{{ include "moo414.labels" . | indent 4 }}
{{- end -}}
