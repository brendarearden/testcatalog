{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "moo890.serviceAccountName" . }}
  labels:
{{ include "moo890.labels" . | indent 4 }}
{{- end -}}
