{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "bar282.serviceAccountName" . }}
  labels:
{{ include "bar282.labels" . | indent 4 }}
{{- end -}}
