apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "areallylongnamelikereallyreally.fullname" . }}-test-connection"
  labels:
{{ include "areallylongnamelikereallyreally.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "areallylongnamelikereallyreally.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
