{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "areallylongnamelikereallyreally.serviceAccountName" . }}
  labels:
{{ include "areallylongnamelikereallyreally.labels" . | indent 4 }}
{{- end -}}
